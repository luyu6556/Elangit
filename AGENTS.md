# AGENTS.md — 给 Codex / 任何接手的 AI 代理

> **这是本项目的唯一入口文件。** 读完它你能立刻接手；细节一律指向 `docs/`，不要凭记忆猜。
> 最后更新：2026-09-20（**任务 9 已发布上线**）｜ 项目：**Elangit（个人灵感库）**

---

## 0. 先记住三件事

1. **这个项目同时是「用户真在用的工具」和「求职作品集项目」。** 用户是风景园林硕士，正在转产品经理。所以任何改动既要真好用，也要能讲清「需求 → 方案 → 验证 → 迭代」的闭环。**别做只能演示、不能用的小玩意。**
2. **用户要求：需求没问透不要动手。** 追问到约 95% 理解 + 拿到明确同意再执行。**宁可多问，不要返工。** 有反对意见直说，并附理由。
3. **数字必须有出处。** 无法证实的标「存疑」，**绝不编造**。缺失信息用占位符 `【待补充】`/`【待确认】`。这条是这个项目的硬规矩，写在用户画像里。

---

## 1. 这是什么

**一句话**：把散落在各平台的灵感素材（截图、文本）收进来，由 AI 自动摘要分类，需要时按主题快速找回。

- **痛点**：用户刷小红书/公众号存了一堆截图，存得下、**找不回**。所以立足点是「找回」不是「存」。
- **形态**：部署在云端的响应式网页应用（手机 + 电脑同一份数据）。数据与 AI 能力全部用平台云服务，不买服务器、不要域名、不自备 API Key。
- **不做**：不做自建后端、不做账号体系、不做原生 App（见第 8 节，这几条是用户明确定的边界）。

---

## 2. 目录地图

```
AGENTS.md            ← 本文件（唯一入口）
README.md            ← 给人看的项目索引
docs/
  00-项目交接-Elangit.md    ← 项目全貌（比本文件更完整，含户画像与「绝对不能踩的坑」）
  03-个人灵感库-需求概要.md  ← 阶段一需求基线
  04-个人灵感库-PRD.md       ← ★ 需求基线（v11，开发依据）
  06-技术方案.md             ← ★ 设计与实装记录（v10，含数据表、AI 设计、各任务实测）
  05-界面稿.html             ← 界面稿
  构建记录.md                ← ★ 决策日志 #001~#024，作品集/面试主要取料处
  07-个人基础资料（私密）.md  ← 用户个人素材源（简历/作品集/面试话术取料处）★ 私密
  08-产品经理术语手册.md      ← 给用户学习用的手册
  09-简历-产品经理实习版v1.md ← 简历 v1
  screenshots/               ← 13 张验收截图（各任务实测留证）
app/                 ← ★ 完整应用源码（原生 JS，无构建步骤）
data/                ← 数据库快照（见第 6 节）
tools/               ← 一次性工具，**不进公开仓库**：push-to-github.py（导出公开仓库，含脱敏）、
                        build-deploy.py（组装发布件）、make-verify-ui.py（生成验证副本）
```

**磁盘上的副本有四个，用途不同，别改错**（改错会造成真源漂移，本项目已明确过一次）：

| 目录 | 是什么 | 能不能手改 |
|---|---|---|
| `~/Documents/Elan git/` | **项目根 = 唯一真源**（本文件所在处，`config.js` 是真 key） | ✅ 只改这里 |
| `~/Documents/Elangit-公开仓库/` | 公开仓库副本，**已脱敏**，是构建产物（git 仓库，可 push） | ❌ 用 `tools/push-to-github.py --export-to` 重生成 |
| `~/Documents/Elangit-部署/` | 发布件，**保留真 key**，是构建产物（平台上传的就是它） | ❌ 用 `tools/build-deploy.py` 重生成 |
| WorkBuddy 工作区 | 最初开发处，已停用 | ❌ 只读 |

**读文档的顺序**：`AGENTS.md`（本文件）→ `docs/06-技术方案.md` 第 3、4、9 节 → `docs/04-个人灵感库-PRD.md` → 需要查「当时为什么这么定」时翻 `docs/构建记录.md`。

---

## 3. 怎么跑起来

```bash
cd app
python3 _serve.py           # 默认 http://127.0.0.1:8791
```

**为什么不用 `python3 -m http.server`**：它会发 `Last-Modified`，浏览器据此启发式缓存；改完 `store.js` 刷新拿到的还是旧文件（本项目真实踩过一次，表现为「新加的函数是 undefined」）。`_serve.py` 强制 `Cache-Control: no-store`，并且把 `/s/*`、`/i/*` 重写到 `share.html`（分享链接是路径形态）。

**页面**：

| 文件 | 作用 |
|---|---|
| `index.html` | 素材库（首页）：侧栏抽屉/标签 + 卡片栅格 + 搜索 + 筛选 + 收藏夹分享面板 |
| `add.html` | 录入页：粘贴/拖拽/点选 → 本地出图入库 → AI 异步补齐 → 就地改 |
| `item.html` | 素材详情：原图（可放大）/原文/来源/摘要/抽屉/标签/备注/换封面/改动对照/删除 |
| `share.html` | 访客只读页：认 `/s/令牌`、`/i/令牌`、`?t=令牌`、`#令牌` 四种入口 |
| `stats.html` | 数据页（仅所有者）：验收进度 + 6 个指标面板 |
| `stats.js` | 门槛与统计的纯函数（数据页用）。**门槛值只在这里定义一处**；抽出来是为了让临界样本可断言（见第 4 节第 10 条） |

**所有者模式**：右上角「解锁编辑」输口令。口令哈希写在 `app/config.js`（纯前端比对，解锁状态记 localStorage）。**这是刻意的降级，见第 4 节第 5 条。**

### 怎么重新发布（任务 9 已上线）

线上地址 **https://elangit.app.workbuddy.host/**。改完代码要同步上线时，顺序是：

```bash
python3 tools/build-deploy.py        # ① 组装发布件到 ~/Documents/Elangit-部署/（会自查）
# ② 用发布能力上传该目录 —— 必须带 appId，见下面第 3 点
```

三条硬要求，**漏一条就出前面说过的坑**：

1. **入口是 `python3 _serve.py`，不是静态托管。** 分享链接 `/s/令牌` 靠服务端转发到 `share.html`（PRD 4.6 选了路径形态），纯静态托管做不到。
2. **`_serve.py` 用 `ThreadingTCPServer`，别退回单线程 `TCPServer`。** 前面有反向代理，代理的健康检查与浏览器并发请求会互相排队，表现为「偶发卡住几秒」。
3. **必须复用应用 ID `wbapp_az0Z1pxT1CjCvbUNffduqc`，绝不能新建应用。** 云服务对请求来源做 **Origin 精确匹配**，只有该 ID 对应的保留域名被放行；新建应用会拿到不匹配的域名，页面能打开但**一条数据都读不出来**（见第 4 节第 2 条）。

**两条流水线方向相反，不要混用**：

| 脚本 | 去处 | 凭据 | 原因 |
|---|---|---|---|
| `tools/push-to-github.py --export-to DIR` | 公开**仓库** | **脱敏** | 仓库是源码快照，不需要能跑 |
| `tools/build-deploy.py` | 公开**运行的应用** | **保留真 key** | 纯前端直连库 + 分享链接要给外人读，架构上不存在「只有所有者持有 key」 |

**发布前先想清楚的一件事**：key 随页面下发到公网是**架构决定的**（不是疏忽），缓解项只有可被伪造的 Origin 校验。**撤销发布不收回 key**；云库不能当唯一副本，定期导出是必要兜底。理由与已接受的风险见第 4 节第 5 条与第 10 节。

---

## 4. 接手前必须知道的 10 个事实

1. **云服务不是 Supabase、不是 CloudBase。** 用 `@tencent-ai/workbuddy-cloud-sdk`（CDN 引入，`WorkBuddyCloud.createWorkBuddyCloud({endpoint, publishableKey})`）。**别手写 `fetch` 打 `/.cloud/**`，那不对。**
2. **应用 ID `wbapp_az0Z1pxT1CjCvbUNffduqc`，端点 `https://elangit.app.workbuddy.host`。** 应用 ID 复用才能保住域名与云服务校验的匹配关系（服务端强制 Origin 精确匹配）。
   - **2026-09-20 实测的表现形式**（发布前做的「同文件、同进程、只换来源」对照）：`http://127.0.0.1:8796` 正常读到 3 条；换成 `http://192.168.1.104:8796`（本机局域网）就整页停在「加载中…」，约 20 秒后报 `TypeError: Failed to fetch`，而**浏览器控制台一条错都不报**。
   - 所以**发布必须带 `appId` 复用**，不能新建应用；否则症状是「发布成功、链接能开、但读不出任何数据」，且没有报错可查。线上正常读库是这个匹配关系成立的标志。
3. **7 张表**：`items` / `item_images` / `categories` / `tags` / `tag_groups` / `settings` / `events`。结构见 `data/schema.sql`（从线上库实际导出）与 `docs/06-技术方案.md` 第 3 章。
4. **图片是 base64 直接存在 `item_images.data_base64` 里的**，没用对象存储。原因：平台 Storage 只对已登录用户开放，与「不做账号体系 + 只读分享给外人」冲突。列表页只取 `items.cover_thumb`（400px），**不要把 `data_base64` 混进列表查询**——一次 20 条会拉下几十 MB。
5. **所有者保护是纯前端的，且这是已接受的决定（R-1）。** 口令哈希在前端，比对在浏览器里做。它拦的是误操作与路人，**不是攻击者**。所有写操作在 `store.js` 层过一道 `guard()`（新增写入口时自动被挡住，别把门禁写在各页面里）。
6. **`items.ai_raw` 是 AI 原值快照**，用户改动**永远不覆盖它**。F2-8「改动对照」和 A2「标签可用率」全靠它。存量素材（v7 之前录入的）这一列是 NULL，此时**必须如实说「没留下来」，不能写成「没改过」**——后者是在断言一个不知道的事实。
7. **埋点只记「不可派生」的过程量。** 14 条里实写 10 条（E1/E2/E3/E5/E6/E7/E8/E9/E13，以及 **E14**）；E4/E11/E12 从 `ai_raw` 与现值**派生**；E10 明确不做。动手加事件前先问：「这件事能不能从库里的当前状态算出来？」能算的不要落库——少一处写入就少一处会漂移的副本。
   - **E14 `search_attempt_end` 是 A4 的分母**（2026-09-20 新增）：一次检索必须收场为 `found` / `timeout` / `abandoned` 之一，**后两者都算失败**。旧口径只对「点开过的」算比例，会让找回成功率系统性偏高——发起检索但没点开的那些次数一条都留不下。
   - `beginSearch()` 生成的是**页面会话级** id，**不要改用 `sid()`**：那是设备级持久值，刷新不变，会把不同次的检索混成一个 id。
   - **限制**：`abandoned` 只能在页面卸载时触发，而浏览器会取消卸载时未完成的 `fetch`，所以它会少计。估偏差：同窗口内 E6 条数 − E14 非 abandoned 条数。
   - **别用 E6/E7 算 A4 的比例**（改版前的老埋点没有检索 id，配不成对；数据页对这种情况有专门文案）。
8. **`diff.js` 是详情页与数据页共用的比较规则。** 不要在某一页里另写一份，否则会出现「详情页说改了 3 处、数据页说 2 处」。
   - 数据页取数必须带 `ai_summary` / `ai_caption` / `ocr_text`：`diff.fields()` 拿这三列跟 `ai_raw` 比，少一列，「现值」恒为空串 → 那三行的可用率恒为 0%。**不报错，但数字全错**（2026-09-20 修过这个 bug，`REPORT_COLS` 里已补）。
9. **`cards.js` 是素材库与分享页共用的公开渲染模块。** 分享页是对外的脸，复制一份 HTML 迟早和主站长歪。
10. **数据页的门槛只有一个来源：`stats.js` 的 `GATES`。** 不要在页面里另写 `n < 10` 这类比较；不到门槛就直书「样本不足（n / 目标）」，**一个百分比都不给**（3 个样本上的 70% 毫无意义——把数字当结论引用比没有数字更糟）。两条配套口径：**计数不属于被禁的「结论」**（「库里 3 条」是事实），所以照常显示；**A13 到 30 条只显示中性的「待人工复盘」**，因为素材够 30 条只说明「可以复盘了」，不等于复盘做过了，而数据页读不到 `构建记录.md`，不能代判。

---

## 5. 当前进度与下一步

**已完成**：任务 1~9。应用可本地运行、可录入、可检索、可编辑、可分享、有埋点与数据页，**并已在云端上线**：https://elangit.app.workbuddy.host/（手机可直接打开）。开发任务全部结束。

**发布时验掉的两件事**（都是之前挂着的未知数，别重复怀疑）：

- ✅ **路径式分享链接在平台上成立。** 平台跑的是我们自己的 Python 服务（不是静态托管），`/s/令牌` → `share.html` 的服务端重写在线上有效：`curl https://elangit.app.workbuddy.host/s/2zs9q7nzpcz4avg8` 返回 200 / 18186 B，浏览器打开显示「共 3 条素材 · 只读」。**不需要退化成 `?t=令牌`。**
- ✅ **线上读得到库。** 复用应用 ID 后保留域名与云服务 Origin 匹配，首页正常读出 3 条素材；手机视口 390×844 无横向溢出；各页 JS 报错 0。

**接手后的下一件事是「真用起来」，不是继续写代码**：

1. 让用户真的用起来——A12「连续两周不断」是整条验收里最重要的一条，只能靠真实使用。
2. 录满 30 条后做一次抽屉与标签复盘（A13），结论记进 `docs/构建记录.md`。
3. PRD 第 10 章多数验收项（A1/A2/A4/A12/A13）**都要真实数据才判得了**，开发阶段宣布不了通过。
4. 库内容值钱起来之后，**定期导出**（`data/tools/export-all.html`）——key 已随页面公开、且撤销发布不收回，云库不能当唯一副本。

---

## 6. `data/` 里有什么，怎么还原

| 文件 | 内容 |
|---|---|
| `schema.sql` | 7 张表的 DDL + 索引 + GRANT + RLS 策略（从线上库实际导出，可直接重建） |
| `dump.sql` | 全库数据的 INSERT（含 id）。**含 `items.cover_thumb` 与图片 base64**，所以有 ~1MB |
| `items.json` | 素材的**文本字段**（不含图片与缩略图），给人/agent 直接读 |
| `images/` | 从 `item_images.data_base64` 解出来的真实图片文件 |
| `images/_manifest.csv` | 图片清单（item_id、seq、尺寸、字节数、`ai_rect`） |
| `elangit-dump.json` | 原始导出（页面直出，未加工的 JSON） |
| `originals/` | 用户提供的两张原始截图（验证 AI 裁切用的） |

**还原顺序**：先跑 `schema.sql`，再跑 `dump.sql`。

⚠️ **还原的两个坑**：① 6 张表的 `id` 是 `GENERATED ALWAYS AS IDENTITY`，显式写 id 必须带 `OVERRIDING SYSTEM VALUE`，`dump.sql` 里已经带了；② 灌完数据要 `setval` 重置序列（`dump.sql` 每个表末尾已附），否则后续新增会撞号。

**快照日期**：2026-09-20。库内 3 条素材 / 3 张图 / 6 个抽屉 / 32 个标签 / 5 个标签分组 / 1 条事件。

---

## 7. 已知的坑（都真实踩过，别再踩第二次）

| 坑 | 症状 | 原因与修法 |
|---|---|---|
| **路径式链接下页面变空壳** | `/s/令牌` 打开后卡在「正在打开…」，**但 console 一条错都不报** | 浏览器把页面目录认成 `/s/`，页内所有相对路径（`style.css`/`config.js`/…）被解析到 `/s/*` 全部 404，只有 CDN 那个绝对地址的 SDK 加载成功。修法：`<head>` 最前面、任何资源之前动态写 `<base>` 指回应用目录。**症状是空壳不是报错，必须逐项断言 `window.Elangit` 各模块是否存在** |
| **CSS 类名撞车** | 空字段各占 200+px、内容还居中 | `.empty{text-align:center;padding:80px 20px}` 是整页空态，却被 `class="ro empty"` / `"csum empty"` / `"sum empty"` 继承了内边距。修法：整页空态改名 `.emptystate`。**教训：给「状态」起类名别用 `empty` 这类通用词** |
| **跳转前发的埋点必然丢失** | 事件代码看着对，库里一条没有 | 浏览器在页面卸载时**取消未完成的 `fetch`**，所以「点结果 → 埋点 → `location.href` 跳详情页」这条事件系统性全部丢失（而它是 A4 唯一判据）。修法：先把写入 await 掉（给 400ms 预算）再跳转 |
| **会话类埋点一条都没有** | 看起来像功能没做 | 顺序写反：先落 localStorage 的「今天已记过」标记、再写库；写入失败时标记已落，把自己挡掉。修法：**先写库、成功后落标记** |
| **`track.session()` 只对所有者生效** | 访客访问一次，标记照样落上 | 同上。`event()` 对访客返回 `false`，所以判断写入结果 |
| **静态服务缓存旧 JS** | 改完刷新还是旧行为，报「新函数 undefined」 | 裸 `http.server` 发 `Last-Modified`。修法：`_serve.py` 强制 `Cache-Control: no-store` |
| **`click` 返回成功但页面没反应**（自动化验证时） | 工具报 `✓ Done`，什么都没发生 | 元素在视口外，点击坐标落在空处；CLI 的 `click` 不自动滚动。修法：先 `scrollIntoView` + `elementFromPoint` 确认真的能点到 |
| **窄屏顶栏被挤坏** | 中文被压成逐字竖排、整页多出横向滚动 | 不换行的 flex 子项被 `flex-shrink` 压到内容宽度以下。修法：给不该压的子项加 `flex:none`，文本加 `white-space:nowrap`，并补分级降级 |
| **`item.html` 的左栏按钮点不到** | 宽屏正常，577px 高的窗口怎么滚都点不到「换封面」 | 左栏 `position:sticky` + 很高的封面图（709×860 裁切后约 530px），把按钮推到 679px 处且整栏被钉住，于是按钮永远停在视口之外。**修法不是调数值而是改结构**：`.col-media{max-height:calc(100vh - 92px);overflow-y:auto}`，栏高永远不超过视口，装不下就在栏内滚。同类问题：浏览页 `aside` 也用了 `max-height + overflow-y` |
| **改了导出器却没 dry-run** | 导出时才 `NameError` 崩掉；或者公开仓库里多出不该有的文件（截图、验证台、操作说明） | 两种都真发生过：① 改排除规则时只加了引用、定义丢了 → 立刻 `--dry-run` 才暴露；② 排除规则原本只认**文件名**，而验证台的截图放在 `_verify_shots/`（截图文件名不带前缀）→ 会被静默带上去。修法：按**相对路径的每一段**判定，并且**每次改完立刻** `python3 tools/push-to-github.py --dry-run` 核对文件总数与清单 |
| **`grep` 的假阴性**（本机是 BSD grep） | 「查不到」某个符号，于是误判「代码里没有」 | BSD grep 的**基本正则不支持 `\|` 交替**，`grep -n "E6\|E7"` 会静默返回空。修法：一律用 `grep -E "E6|E7"` |
| **同一批改多个位置，有的没落地**（用 AI 编辑工具时） | 工具逐条报「成功」，但回读文件发现少改了一处、或者同一行写了两遍 | 一次消息里发多条编辑时，出现过被覆盖/丢失/重复（改 `push-to-github.py` 的排除规则时定义那半段丢了、改 AGENTS.md 目录地图时版本号没改上、坑表某行被写了两遍）。**修法：改完必须回读文件核对，尤其批量编辑之后；宁可一次只改一处** |

---

## 8. 不要做的事（用户明确定的边界，写在这一节就是要你顶住）

1. **不要加跨平台自动抓取**（小红书/公众号）。技术与合规上都有硬限制，第一版奔着它去会卡死。**用户可能会反复提，要顶住。**
2. **不要加账号体系 / 多用户。** 第一版只有一个用户。数据结构已预留 `user_id`——「预留接口」和「实现功能」是两件事，要讲清楚。
3. **不要把录入路径做长。** 单条 ≤30 秒是这类工具的最大死因：灵感出现时用户在刷手机，操作一麻烦，用两周就放弃。AI 处理**不计入阻塞时长**（本地 ≤1s 出图入库，AI 异步补齐，60s 超时降级为手填）。
4. **不要只靠 AI 自由标签。** 标签必须优先复用已有标签库（提示词里带枚举），抽屉是**封闭集合**、AI 只能从中选一个。前 50 条要手动复盘一次。
5. **不要做成「另一个收藏夹」。** 任何功能决策都要回到「这样能让我更快**找回**吗」。
6. **不要扩大范围。** 用户容易在讨论中加想法，每次都问「这属于第一版吗」。
7. **不要在没验证的情况下宣称「已验证」。** 用户上一个项目（AI 求职助手）的教训就是从头到尾没做过一次真实验证。这个项目必须真的用起来、并留下数据。
8. **不要把 iCloud 云盘当产品数据层。** 网页应用读不了它。

---

## 9. 交付纪律（用户在这个项目上反复强化的）

- **每条结论挂证据**：实测数字 + 对照基准。没有基准的「看起来是对的」不算结论。
- **数值类结论要多轮**：同一张图 AI 给的主体矩形宽度实测在 59%~70% 之间抖动（±10 个百分点），单次测量不能当结论。**这是已知限制，不是待修 bug。**
- **如实记限制**，并且写在显眼处：前端口令不是安全边界；埋点是前端记录不是可信审计；E10 不做所以拿不到「分享实际价值」的量化。
- **改完文档要同步**：`docs/构建记录.md` 追加一条（背景 / 问题 / 回答或决策 / 理由 / 影响文件 / 下一步），`docs/04-个人灵感库-PRD.md` 与 `docs/06-技术方案.md` 若口径变了要升版本并写明变更依据。
- **注意入口**：`app/_verify_*.html` 是**一次性验证台**（结论落地前不要删，但它们不属于产品代码，改产物时不要动它们）。

---

## 10. 敏感信息提醒

本目录同时是 **Codex 的工作区**（要真值）和 **公开仓库的上游**（不能带真值）。两者的差异由 `tools/push-to-github.py` 在上传时处理，**不要手工同步、不要在本地 `git push`**。

**上传时被替换掉的（磁盘上仍是真值）**：

| 内容 | 公开仓库里的样子 |
|---|---|
| `app/config.js` · `publishableKey` | `wbpk_<你的 publishableKey>` |
| `app/config.js` · `ownerPasscodeHash` | `''` |
| `data/tools/export-all.html` · `publishableKey` | `wbpk_<你的 publishableKey>` |
| `docs/09-简历…md` · 手机号 / 邮箱 | `【你的手机号】` / `【你的邮箱】` |

**上传时被整个排除的**：`docs/07-个人基础资料（私密）.md`、`data/dump.sql`、`data/items.json`、`data/elangit-dump.json`、`data/images/`、`data/originals/`。

**为什么必须这么做**：

- 数据库 7 张表的 RLS 策略全是 `USING(true)`，且已 GRANT 给 `anon`。也就是说，**拿到 `endpoint` + `publishableKey` 的人可以直接读写全库**（不是只读）。
- 有一个缓解因素：服务端强制 Origin 精确匹配，纯脚本调用需要伪造 `Origin` 才能过。但这是减速带，不是墙——**能伪造**。
- **⚠️ 2026-09-20 起，key 确实在公网上了**（任务 9 已发布）。发布件必须保留真 key：纯前端直连库，且分享链接要让**收到链接的人**也读得到数据，所以本架构下不存在「只有所有者持有 key」。**别把「公开仓库已脱敏」误读成「key 是秘密」**——脱敏只是不让仓库成为第二个泄露点。
- 由此两条后果：**撤销发布不收回 key**（先前拿到页面的人手里那份仍然可用，真要收回只能轮换 key）；**云库不能当唯一副本**（key 一旦被滥用，最坏情况是全库被清，所以素材值钱起来后要定期导出，用 `data/tools/export-all.html`）。

因此 `publishableKey` 上传前一律置成占位符。带来的后果是**从 GitHub clone 下来的那份跑不起来**，这是预期的：要跑就用磁盘上这份，或向用户索取凭据。

- `docs/07-个人基础资料（私密）.md` 是用户的私人素材（个人经历、简历原始材料），**任何时候都不要提交**。
- 若将来用户希望公开仓库里的演示能直接跑通，**不要直接把真 key 塞回 config.js**——正确做法是给公开版本单独建一个演示库（新的 app + 新的 key），两边数据隔离后再展示。

