# AGENTS.md — 给 Codex / 任何接手的 AI 代理

> **这是本项目的唯一入口文件。** 读完它你能立刻接手；细节一律指向 `docs/`，不要凭记忆猜。
> 最后更新：2026-09-23（**翻阅页三处修正：点旁侧卡＝滚到那一张（不翻面，`pointer-events` 子覆盖祖先的假防线），滚轮由「一次甩动」改为「连续滚动」（1:1 跟手、逐格翻页、不封顶 —— 推翻 #047），验证台自身加固（整轮 try/catch + 正向收尾标记）。`_verify_projects.html` 157 条全绿**）｜ 项目：**Elangit（个人灵感库）**

---

## 0. 先记住三件事

1. **这个项目同时是「用户真在用的工具」和「求职作品集项目」。** 用户是风景园林硕士，正在转产品经理。所以任何改动既要真好用，也要能讲清「需求 → 方案 → 验证 → 迭代」的闭环。**别做只能演示、不能用的小玩意。**
2. **用户要求：需求没问透不要动手。** 追问到约 95% 理解 + 拿到明确同意再执行。**宁可多问，不要返工。** 有反对意见直说，并附理由。
3. **数字必须有出处。** 无法证实的标「存疑」，**绝不编造**。缺失信息用占位符 `【待补充】`/`【待确认】`。这条是这个项目的硬规矩，写在用户画像里。

---

## 1. 这是什么

**一句话**：把散落在各平台的灵感素材（截图、文本）收进来，由 AI 自动摘要分类，需要时按主题快速找回。

- **痛点**：用户刷小红书/公众号存了一堆截图，存得下、**找不回**。所以立足点是「找回」不是「存」。
- **形态**：部署在云端的响应式网页应用（手机 + 电脑同一份数据）。数据与 AI 能力全部用平台云服务，不买服务器、不要域名、不自备 API Key。
- **不做**：不做自建后端、不做账号体系、不做原生 App（见第 8 节，这几条是用户明确定的边界）。

---

## 2. 目录地图

```
AGENTS.md            ← 本文件（唯一入口）
README.md            ← 给人看的项目索引
docs/
  00-项目交接-Elangit.md    ← 项目全貌（比本文件更完整，含户画像与「绝对不能踩的坑」）
  03-个人灵感库-需求概要.md  ← 阶段一需求基线
  04-个人灵感库-PRD.md       ← ★ 需求基线（v15，开发依据）
  06-技术方案.md             ← ★ 设计与实装记录（v21，含数据表、AI 设计、各任务实测）
  05-界面稿.html             ← 界面稿（设计变量与 app/style.css 对齐；首页结构**早已不同构**，见第 4 节事实 16）
  构建记录.md                ← ★ 决策日志 #001~#050，作品集/面试主要取料处
  07-个人基础资料（私密）.md  ← 用户个人素材源（简历/作品集/面试话术取料处）★ 私密
  08-产品经理术语手册.md      ← 给用户学习用的手册
  09-简历-产品经理实习版v1.md ← 简历 v1
  screenshots/               ← 13 张验收截图（各任务实测留证）
features/            ← ★ 分功能的功能包，**一条新功能的所有文档都放这里**（不散进 docs/）
  项目灵感筛选/       ← 一期：`PRD.md`（v0.7，已定分两期）、`第一期开发交接单.md`、`项目修改记录.md`（#001~#030）
app/                 ← ★ 完整应用源码（原生 JS，无构建步骤）
  projects/          ← 项目灵感筛选一期的三个页面（列表＋新建编辑／工作台／快速翻阅）＋ `projects.js`／`projects.css`；
                        **写入一律经 `store.js`，页面不直连数据库**；翻阅页的几何参数是 `projects.css` 里的 `--deck-*`，由 JS 读回
data/                ← 数据库快照（见第 6 节）；`data/migrations/` 放线上迁移脚本，每条迁移一份并附结构回读 SQL
tools/               ← 一次性工具，**不进公开仓库**：push-to-github.py（导出公开仓库，含脱敏）、
                        build-deploy.py（组装发布件）、make-verify-ui.py（生成验证副本）
```

**磁盘上的副本有四个，用途不同，别改错**（改错会造成真源漂移，本项目已明确过一次）：

| 目录 | 是什么 | 能不能手改 |
|---|---|---|
| `~/Documents/Elan git/` | **项目根 = 唯一真源**（本文件所在处，`config.js` 是真 key） | ✅ 只改这里 |
| `~/Documents/Elangit-公开仓库/` | 公开仓库副本，**已脱敏**，是构建产物（git 仓库，可 push） | ❌ 用 `tools/push-to-github.py --export-to` 重生成 |
| `~/Documents/Elangit-部署/` | 发布件，**保留真 key**，是构建产物（平台上传的就是它） | ❌ 用 `tools/build-deploy.py` 重生成 |
| WorkBuddy 工作区 | 最初开发处，已停用 | ❌ 只读 |

**读文档的顺序**：`AGENTS.md`（本文件）→ `docs/06-技术方案.md` 第 3、4、9 节 → `docs/04-个人灵感库-PRD.md` → 需要查「当时为什么这么定」时翻 `docs/构建记录.md`。**若手上的任务属于「项目灵感筛选」，先读 `features/项目灵感筛选/第一期开发交接单.md` 与 `项目修改记录.md`（最新一条在最下面），再读相关源码；不要凭摘要改库。**

---

## 3. 怎么跑起来

```bash
cd app
python3 _serve.py           # 默认 http://127.0.0.1:8791
```

**为什么不用 `python3 -m http.server`**：它会发 `Last-Modified`，浏览器据此启发式缓存；改完 `store.js` 刷新拿到的还是旧文件（本项目真实踩过一次，表现为「新加的函数是 undefined」）。`_serve.py` 强制 `Cache-Control: no-store`，并且把 `/s/*`、`/i/*` 重写到 `share.html`（分享链接是路径形态）。

**页面**：

| 文件 | 作用 |
|---|---|
| `index.html` | **首页 = 全库翻阅**：九张卡的全库牌堆，左上「新建项目」/右上「添加素材」/底部居中「进入素材库」；没选项目时点爱心弹选项目浮层（不静默收藏） |
| `library.html` | **素材库**：侧栏抽屉/标签 + 卡片栅格 + 搜索 + 筛选 + 收藏夹分享面板（原首页素材库整块迁来） |
| `add.html` | 录入页：粘贴/拖拽/点选 → 本地出图入库 → AI 异步补齐 → 就地改。**只填网址时先抓原文标题/首图**（`/api/meta`、`/api/img`） |
| `item.html` | 素材详情：该条实际拥有的原图（可放大）/原文/来源/摘要/**设计说明总结**/抽屉/标签/备注/换封面/改动对照/删除 |
| `share.html` | 访客只读页：认 `/s/令牌`、`/i/令牌`、`?t=令牌`、`#令牌` 四种入口 |
| `stats.html` | 数据页（仅所有者）：验收进度 + 6 个指标面板 |
| `stats.js` | 门槛与统计的纯函数（数据页用）。**门槛值只在这里定义一处**；抽出来是为了让临界样本可断言（见第 4 节第 10 条） |
| `_page_meta.py` | **抓目标网页标题/描述/首图/正文**（只填网址的采集，纯标准库）。正文抽取去导航与操作位噪声、按标签配对计数取主体、双语站去掉英文半篇、上限 8000 字。SSRF 防护：只放行公网地址、逐跳重校验、超时与字节上限（见第 4 节） |

**所有者模式**：右上角「解锁编辑」输口令。口令哈希写在 `app/config.js`（纯前端比对，解锁状态记 localStorage）。**这是刻意的降级，见第 4 节第 5 条。**

### 怎么重新发布（任务 9 已上线）

线上地址 **https://elangit.app.workbuddy.host/**。改完代码要同步上线时，顺序是：

```bash
python3 tools/build-deploy.py        # ① 组装发布件到 ~/Documents/Elangit-部署/（会自查）
# ② 用发布能力上传该目录 —— 必须带 appId，见下面第 3 点
```

三条硬要求，**漏一条就出前面说过的坑**：

1. **入口是 `python3 _serve.py`，不是静态托管。** 分享链接 `/s/令牌` 靠服务端转发到 `share.html`（PRD 4.6 选了路径形态），纯静态托管做不到。
2. **`_serve.py` 用 `ThreadingTCPServer`，别退回单线程 `TCPServer`。** 前面有反向代理，代理的健康检查与浏览器并发请求会互相排队，表现为「偶发卡住几秒」。
3. **必须复用应用 ID `wbapp_az0Z1pxT1CjCvbUNffduqc`，绝不能新建应用。** 云服务对请求来源做 **Origin 精确匹配**，只有该 ID 对应的保留域名被放行；新建应用会拿到不匹配的域名，页面能打开但**一条数据都读不出来**（见第 4 节第 2 条）。

**两条流水线方向相反，不要混用**：

| 脚本 | 去处 | 凭据 | 原因 |
|---|---|---|---|
| `tools/push-to-github.py --export-to DIR` | 公开**仓库** | **脱敏** | 仓库是源码快照，不需要能跑 |
| `tools/build-deploy.py` | 公开**运行的应用** | **保留真 key** | 纯前端直连库 + 分享链接要给外人读，架构上不存在「只有所有者持有 key」 |

**发布前先想清楚的一件事**：key 随页面下发到公网是**架构决定的**（不是疏忽），缓解项只有可被伪造的 Origin 校验。**撤销发布不收回 key**；云库不能当唯一副本，定期导出是必要兜底。理由与已接受的风险见第 4 节第 5 条与第 10 节。

---

## 4. 接手前必须知道的 16 个事实

1. **云服务不是 Supabase、不是 CloudBase。** 用 `@tencent-ai/workbuddy-cloud-sdk`（CDN 引入，`WorkBuddyCloud.createWorkBuddyCloud({endpoint, publishableKey})`）。**别手写 `fetch` 打 `/.cloud/**`，那不对。**
2. **应用 ID `wbapp_az0Z1pxT1CjCvbUNffduqc`，端点 `https://elangit.app.workbuddy.host`。** 应用 ID 复用才能保住域名与云服务校验的匹配关系（服务端强制 Origin 精确匹配）。
   - **2026-09-20 实测的表现形式**（发布前做的「同文件、同进程、只换来源」对照）：`http://127.0.0.1:8796` 正常读到 3 条；换成 `http://192.168.1.104:8796`（本机局域网）就整页停在「加载中…」，约 20 秒后报 `TypeError: Failed to fetch`，而**浏览器控制台一条错都不报**。
   - 所以**发布必须带 `appId` 复用**，不能新建应用；否则症状是「发布成功、链接能开、但读不出任何数据」，且没有报错可查。线上正常读库是这个匹配关系成立的标志。
3. **9 张表**：原 7 张是 `items` / `item_images` / `categories` / `tags` / `tag_groups` / `settings` / `events`；**2026-09-23 新增 `projects` 与 `project_items`**（项目灵感筛选一期，见 `features/项目灵感筛选/`）。结构见 `data/schema.sql` 与 `docs/06-技术方案.md` 第 3 章。
   - **动线上库的规矩**：执行走云服务的 SQL 工具，**单次只接受一条语句**（多条报 `cannot insert multiple commands into a prepared statement`）——所以带 `begin; … commit;` 的迁移**不能整体执行、没有原子性**，且建表／建索引／建策略都不幂等。**回读才是成功的判据**：`pg_class.relrowsecurity`（注意 `describe_table` 的 `RLSOn` 字段对**所有**表都返回 `false`，不可信）、`pg_constraint`+`pg_get_constraintdef`、`pg_indexes`、`information_schema.role_table_grants`。案例见构建记录 #039。
4. **图片是 base64 直接存在 `item_images.data_base64` 里的**，没用对象存储。原因：平台 Storage 只对已登录用户开放，与「不做账号体系 + 只读分享给外人」冲突。列表页只取 `items.cover_thumb`（400px），**不要把 `data_base64` 混进列表查询**——一次 20 条会拉下几十 MB。
5. **所有者保护是纯前端的，且这是已接受的决定（R-1）。** 口令哈希在前端，比对在浏览器里做。它拦的是误操作与路人，**不是攻击者**。所有写操作在 `store.js` 层过一道 `guard()`（新增写入口时自动被挡住，别把门禁写在各页面里）。
6. **`items.ai_raw` 是 AI 原值快照**，用户改动**永远不覆盖它**。F2-8「改动对照」和 A2「标签可用率」全靠它。存量素材（v7 之前录入的）这一列是 NULL，此时**必须如实说「没留下来」，不能写成「没改过」**——后者是在断言一个不知道的事实。
7. **埋点只记「不可派生」的过程量。** 原 14 条指标里实写 10 条（E1/E2/E3/E5/E6/E7/E8/E9/E13，以及 **E14**）；E4/E11/E12 从 `ai_raw` 与现值**派生**；E10 明确不做。另有 E15/E16 两条**诊断事件**（AI 开始 / 外层异常退出），用于区分「没开始」与「跑了没回来」，不服务现有验收指标。现共实写 **12 条**。动手加事件前先问：「这件事能不能从库里的当前状态算出来？」能算的不要落库——少一处写入就少一处会漂移的副本。
   - **E14 `search_attempt_end` 是 A4 的分母**（2026-09-20 新增）：一次检索必须收场为 `found` / `timeout` / `abandoned` 之一，**后两者都算失败**。旧口径只对「点开过的」算比例，会让找回成功率系统性偏高——发起检索但没点开的那些次数一条都留不下。
   - `beginSearch()` 生成的是**页面会话级** id，**不要改用 `sid()`**：那是设备级持久值，刷新不变，会把不同次的检索混成一个 id。
   - **限制**：`abandoned` 只能在页面卸载时触发，而浏览器会取消卸载时未完成的 `fetch`，所以它会少计。估偏差：同窗口内 E6 条数 − E14 非 abandoned 条数。
   - **别用 E6/E7 算 A4 的比例**（改版前的老埋点没有检索 id，配不成对；数据页对这种情况有专门文案）。
8. **`diff.js` 是详情页与数据页共用的比较规则。** 不要在某一页里另写一份，否则会出现「详情页说改了 3 处、数据页说 2 处」。
   - 数据页取数必须带 `ai_summary` / `ai_caption` / `ocr_text` / `ai_digest`：`diff.fields()` 拿这几列跟 `ai_raw` 比，少一列，「现值」恒为空串 → 对应行的可用率恒为 0%。**不报错，但数字全错**（2026-09-20 修过这个 bug，`REPORT_COLS` 里已补；**2026-09-22 同一个坑又犯了一次**——新加的 `digest` 列漏在 `REPORT_COLS` 外。**给 `diff.fields()` 加字段时，必须同时检查取数列，这两处没有机制保证同步。**）。
9. **`cards.js` 是素材库与分享页共用的公开渲染模块。** 分享页是对外的脸，复制一份 HTML 迟早和主站长歪。
10. **数据页的门槛只有一个来源：`stats.js` 的 `GATES`。** 不要在页面里另写 `n < 10` 这类比较；不到门槛就直书「样本不足（n / 目标）」，**一个百分比都不给**（3 个样本上的 70% 毫无意义——把数字当结论引用比没有数字更糟）。两条配套口径：**计数不属于被禁的「结论」**（「库里 3 条」是事实），所以照常显示；**A13 到 30 条只显示中性的「待人工复盘」**，因为素材够 30 条只说明「可以复盘了」，不等于复盘做过了，而数据页读不到 `构建记录.md`，不能代判。
11. **耗时埋点的起点必须落在「这一次操作」上，不能落在「页面加载」上。** `pageOpenedAt = Date.now()` 再算 `now - pageOpenedAt` 当「本次操作耗时」，在**一次会话连录多条**时会累加——首次真实录入实测 16 条连录从 27.8s 单调涨到 45.5s（每条 +1.2s），而真实提交只花 200ms，**差两个数量级**，且数字看着合理所以没人怀疑。修法：起点是「本条的第一个输入动作」（`itemStartedAt`），E5 记 `ms_from` 区分新旧口径，统计时**只认新口径样本**、旧口径宁可不计也不掺。
12. **「可用率/修改率」这类指标，空值不能被算成合格。** `changed = before !== after` 这种判定，在「AI 什么都没产出」（`before` 和 `after` 都是空串）时会判 `changed=false`，于是「AI 没产出」被读成「AI 给的东西没被改过 = 可用」——**会自我实现的假通过**。修法：分母只算「AI 在该字段实际产出过非空内容」的样本（`diff.summary()` 给 `produced`/`empty`），产出为 0 时按「样本不足」处理、不给百分比。与第 10 条同源：**把「没数据」读成「数据好」，比没数据更糟**。
13. **`loadIndex()` 与 `fetchItem()` 取的不是同一批列——查真值一律走 `fetchItem()`。** `INDEX_COLS` 为列表页瘦身，故意不含 `page_desc` / `page_text`。用 `loadIndex()` 去查这几列是否为空，会得到「空」这个**假象**，然后据此得出「抽取没生效」的错误结论（2026-09-22 踩过）。同理：**改了被 `_serve.py` import 的模块（如 `_page_meta.py`）必须重启服务**，否则 `/api/meta` 返回的还是启动时的那份旧结构。
14. **测试残留要回查数据库，不能只看界面。** 界面上显示「已还原」不等于库里已还原——2026-09-22 收尾时查出某条的 `ai_digest` 仍留着测试标记。这与 #018「测试数据污染」是同一类问题：**改完数据后直接查库确认，是固定收尾动作。**
15. **`ai_digest` 是「严格校验」出来的，别再把它改回「容错」**（v14，构建记录 #030）。契约：`理念 / 做法 / 效果` 三项**齐全且每项有内容**才接受，顺序乱的重排成固定顺序；缺项或整段没带标签（闲聊、散文）**一律置空**。**不要**出于「宽容」把收敛不了的输出原样存进去——那会让模型的无关回答以「设计说明总结」的身份显示给用户，**等于界面在撒谎**，而这个错误不报错、看不出来。不合规的原文存在 `ai_raw.digestRaw`（`jsonb` 内加键，**没有单独建列**），只用于把详情页空态的原因说准与事后排查，**不进任何统计**；因 `ai_raw.digest` 与 `ai_digest` 写入同源、同时为空，A2 不会把「系统丢弃」算成「用户改过」。改这条要连 PRD F2-12 一起改。

16. **顶栏与列表区的版式有四个「只在某个宽度下才显形」的约束，动手改之前先量**（v15，构建记录 #031）。它们是 2026-09-22 一次性修掉的，**症状都只是难看、不报错**，所以肉眼和 console 都发现不了：
    - **搜索框宽度 = `.bar` 宽 − 计数宽 − 12px**（12px 是 `.bar` 的 `flex gap`）。这个等式就是验收判据，别用「宽度大于某个数」代替。
    - **`.bar` 里不许再放 `.spacer`**，`.search` 也不许再加 `max-width`。原缺陷就是两者相加：`max-width:420px` 卡住上限、`.spacer` 又把剩余空间分走一半 —— 桌面 1440 下搜索框只有 420px、右侧 768px 全空，手机 390 只剩 159px。**教训：一个 flex 行里有两个 `flex:1` 的子项时，先问「它们是不是在抢同一块空间」**（顶栏那个 `.spacer` 是另一回事，它是那一行唯一的可增长项，必须留着）。
    - **`main.list` 在 ≤800px 时是 `column flex` + `align-items:flex-start`，宽度由内容决定。** 列表为空时它会缩到内容宽（实测 390 下只剩 238px），这时量出来的版式**不作数**；量版式前先确认有数据撑着。
    - **顶栏是不换行的 flex**，所以「往顶栏加/减一个元素」的代价必须在 **390px** 上量过再说。logo 里那个 16px 方块标记**已删**（它没有任何语义，而 ≤460px 又会把品牌文字藏掉、只剩它一个），现在 ≤460px 是**整块收起**（`header .logo{display:none}`）。

---

## 5. 当前进度与下一步

**已完成**：任务 1~9。应用可本地运行、可录入、可检索、可编辑、可分享、有埋点与数据页，**并已在云端上线**：https://elangit.app.workbuddy.host/（手机可直接打开）。开发任务全部结束。**2026-09-21 做过一次真实录入测试（16 条 gooood 链接），修正 8 个问题并加服务端抓原文（`_page_meta.py`），详见构建记录 #025。2026-09-22 让 AI 读网页正文并产出「设计说明总结」（详情页），摘要在卡片、总结在详情页，AI 超时上调到 90s，详见构建记录 #026；**同一轮已重新发布上线**（复用原应用 ID，线上代码逐项核过与 `app/` 一致）。**2026-09-22 紧接着做了第二轮：把 digest 的校验从「容错」改成「严格」**（不合规的模型输出不再当总结——改前 12 组输入里有 6 组会以「总结」身份显示，改后 0 组），不合规原文留痕进 `ai_raw.digestRaw`，详情页空态三种改四种，新增验证台 `app/_verify_ai.html`，详见构建记录 #030。**同一轮已完成发布**（复用原应用 ID，链接不变 `https://elangit.app.workbuddy.host/`；线上 5 个文件与本地逐项 SHA256 一致；首页在真实浏览器里读出「7 条」，线上读库正常；`_verify_ai.html` 在线上返回 **404** → 验证件不进发布件的排除规则**实际生效**，不是看代码以为生效）。**公开仓库的脱敏副本已导出并提交（提交信息见构建记录 #030），但还没推上去**（这台机器连不上 github.com，凭据在 GitHub Desktop 里）——下次开工时**先用 GitHub Desktop 把它推掉**，否则公开副本会落后一版。** **2026-09-22 第三轮是版式修正（构建记录 #031）**：首页搜索框取消 `max-width:420px` 并删掉同一行里抢空间的 `.spacer`（实测 1440 → **1150px** / 768 → **613px** / 390 → **328px**，改前分别是 420 / 420 / 159），删掉顶栏 logo 里那个没有语义的 16px 方块标记、窄屏改整块收起，`docs/05-界面稿.html` 按要求同步；新增验证台 `app/_verify_home_ui.html`（69 条断言，67 绿 / **0 红** / 2 条写明原因的「不适用」）。**这一轮也已发布上线**（用户批准后执行）：发布件 18 个文件 / 316.0 KB、三条自查全过，复用原应用 ID、链接不变 `https://elangit.app.workbuddy.host/`；线上 8 个文件与本地逐项 SHA256 一致，四个 `_verify_*` 与 `_verify_shots/` 线上 **404**；线上真实浏览器实测 1440 下搜索框 **1152px**、390 下 **330px**（改前 420 / 159），三个页面**页面错误 0 条**。**

**2026-09-23 起并行进入「项目灵感筛选」一期**（独立功能包，文档在 `features/项目灵感筛选/`）：为设计项目建容器 → 按标签筛选 → 单卡牌堆快翻 → 收藏／取消收藏 → 归档／恢复／删除 → 项目内手动排序；**二期才是 Markdown 导出 + 项目访客只读分享**。数据上是 `projects` + `project_items` 多对多，**绝不把项目 ID／项目收藏／排序写进 `items`**（否则一条素材进多个项目会互相覆盖）。**截至 2026-09-23 的最新状态**：两张表已在线建表，结构／约束／RLS／anon 权限三组回读全部通过，`data/schema.sql` 已同步为 9 张表（#014）；`store.js` 数据访问层完成并**真实云端写入往返回读通过**（#017：跨项目独立、源素材删除级联、删除后二次回读 0 行、`items` 整行不变）；**三个页面（列表／工作台／翻阅）已落地**（#042），翻阅页入场滑动已修（#043），手机端主卡放大到 300×420 且拖拽改分段阻尼（#045），点卡翻面读设计说明总结（#046），Mac 触控板双指横滑 = 翻页（#047）。**紧接着按用户本机反馈修了三处（#048／#049／#050）**：点旁侧卡改为「滚到那一张」不翻面（删掉 `pointer-events` 那句假防线、新增唯一出入口 `goTo()`）；滚轮由「一次甩动」改为**连续滚动**（1:1 跟手、逐格翻页、不封顶、两端即停 —— **推翻 #047 的滚轮模型**）；验证台自身加固（整轮 try/catch + 正向收尾标记 + 如实写临时项目编号）。`_verify_projects.html` **157/157**、`_verify_home_ui.html` **118/113/0/5**（与基线逐项一致）。**仍未做**：人工过一遍手感的一期整体验收；发布与 Git 推送（用户未批准，见第 8 节）。**本轮用户已明确要求导出公开仓库副本（GitHub 推送由用户在 GitHub Desktop 里点）。**

**同一天还收到 Codex 的只读审查**（`交接给WorkBuddy-正文抽取与设计摘要优化-2026-09-22.md`，14 个方向，含 7 个可改、3 个必须先实测、4 个 UI）。#030 是其中的方向 3；**其余方向均未动**。接手下一项前先读那份交接单。

**发布时验掉的两件事**（都是之前挂着的未知数，别重复怀疑）：

- ✅ **路径式分享链接在平台上成立。** 平台跑的是我们自己的 Python 服务（不是静态托管），`/s/令牌` → `share.html` 的服务端重写在线上有效：`curl https://elangit.app.workbuddy.host/s/2zs9q7nzpcz4avg8` 返回 200 / 18186 B，浏览器打开显示「共 3 条素材 · 只读」。**不需要退化成 `?t=令牌`。**
- ✅ **线上读得到库。** 复用应用 ID 后保留域名与云服务 Origin 匹配，首页正常读出 3 条素材；手机视口 390×844 无横向溢出；各页 JS 报错 0。

**接手后的下一件事是「真用起来」，不是继续写代码**：

1. 让用户真的用起来——A12「连续两周不断」是整条验收里最重要的一条，只能靠真实使用。
2. 录满 30 条后做一次抽屉与标签复盘（A13），结论记进 `docs/构建记录.md`。
3. PRD 第 10 章多数验收项（A1/A2/A4/A12/A13）**都要真实数据才判得了**，开发阶段宣布不了通过。
4. 库内容值钱起来之后，**定期导出**（`data/tools/export-all.html`）——key 已随页面公开、且撤销发布不收回，云库不能当唯一副本。

---

## 6. `data/` 里有什么，怎么还原

| 文件 | 内容 |
|---|---|
| `schema.sql` | 9 张表的 DDL + 索引 + GRANT + RLS 策略（前 7 张从线上库实际导出；后 2 张按 2026-09-23 迁移脚本执行并回读后补写，可直接重建） |
| `dump.sql` | 全库数据的 INSERT（含 id）。**含 `items.cover_thumb` 与图片 base64**，所以有 ~1MB |
| `items.json` | 素材的**文本字段**（不含图片与缩略图），给人/agent 直接读 |
| `images/` | 从 `item_images.data_base64` 解出来的真实图片文件 |
| `images/_manifest.csv` | 图片清单（item_id、seq、尺寸、字节数、`ai_rect`） |
| `elangit-dump.json` | 原始导出（页面直出，未加工的 JSON） |
| `originals/` | 用户提供的两张原始截图（验证 AI 裁切用的） |

**还原顺序**：先跑 `schema.sql`，再跑 `dump.sql`。

⚠️ **还原的两个坑**：① 7 张表的 `id` 是 `GENERATED ALWAYS AS IDENTITY`，显式写 id 必须带 `OVERRIDING SYSTEM VALUE`，`dump.sql` 里已经带了（新增的 `project_items` 没有 id 列，主键是 `(project_id, item_id)`；`settings` 也不是 identity）；② 灌完数据要 `setval` 重置序列（`dump.sql` 每个表末尾已附），否则后续新增会撞号。

**快照日期**：2026-09-20。库内 3 条素材 / 3 张图 / 6 个抽屉 / 32 个标签 / 5 个标签分组 / 1 条事件。

---

## 7. 已知的坑（都真实踩过，别再踩第二次）

| 坑 | 症状 | 原因与修法 |
|---|---|---|
| **路径式链接下页面变空壳** | `/s/令牌` 打开后卡在「正在打开…」，**但 console 一条错都不报** | 浏览器把页面目录认成 `/s/`，页内所有相对路径（`style.css`/`config.js`/…）被解析到 `/s/*` 全部 404，只有 CDN 那个绝对地址的 SDK 加载成功。修法：`<head>` 最前面、任何资源之前动态写 `<base>` 指回应用目录。**症状是空壳不是报错，必须逐项断言 `window.Elangit` 各模块是否存在** |
| **CSS 类名撞车** | 空字段各占 200+px、内容还居中 | `.empty{text-align:center;padding:80px 20px}` 是整页空态，却被 `class="ro empty"` / `"csum empty"` / `"sum empty"` 继承了内边距。修法：整页空态改名 `.emptystate`。**教训：给「状态」起类名别用 `empty` 这类通用词** |
| **跳转前发的埋点必然丢失** | 事件代码看着对，库里一条没有 | 浏览器在页面卸载时**取消未完成的 `fetch`**，所以「点结果 → 埋点 → `location.href` 跳详情页」这条事件系统性全部丢失（而它是 A4 唯一判据）。修法：先把写入 await 掉（给 400ms 预算）再跳转 |
| **会话类埋点一条都没有** | 看起来像功能没做 | 顺序写反：先落 localStorage 的「今天已记过」标记、再写库；写入失败时标记已落，把自己挡掉。修法：**先写库、成功后落标记** |
| **`track.session()` 只对所有者生效** | 访客访问一次，标记照样落上 | 同上。`event()` 对访客返回 `false`，所以判断写入结果 |
| **静态服务缓存旧 JS** | 改完刷新还是旧行为，报「新函数 undefined」 | 裸 `http.server` 发 `Last-Modified`。修法：`_serve.py` 强制 `Cache-Control: no-store` |
| **`click` 返回成功但页面没反应**（自动化验证时） | 工具报 `✓ Done`，什么都没发生 | 元素在视口外，点击坐标落在空处；CLI 的 `click` 不自动滚动。修法：先 `scrollIntoView` + `elementFromPoint` 确认真的能点到 |
| **窄屏顶栏被挤坏** | 中文被压成逐字竖排、整页多出横向滚动 | 不换行的 flex 子项被 `flex-shrink` 压到内容宽度以下。修法：给不该压的子项加 `flex:none`，文本加 `white-space:nowrap`，并补分级降级 |
| **`item.html` 的左栏按钮点不到** | 宽屏正常，577px 高的窗口怎么滚都点不到「换封面」 | 左栏 `position:sticky` + 很高的封面图（709×860 裁切后约 530px），把按钮推到 679px 处且整栏被钉住，于是按钮永远停在视口之外。**修法不是调数值而是改结构**：`.col-media{max-height:calc(100vh - 92px);overflow-y:auto}`，栏高永远不超过视口，装不下就在栏内滚。同类问题：浏览页 `aside` 也用了 `max-height + overflow-y` |
| **改了导出器却没 dry-run** | 导出时才 `NameError` 崩掉；或者公开仓库里多出不该有的文件（截图、验证台、操作说明） | 两种都真发生过：① 改排除规则时只加了引用、定义丢了 → 立刻 `--dry-run` 才暴露；② 排除规则原本只认**文件名**，而验证台的截图放在 `_verify_shots/`（截图文件名不带前缀）→ 会被静默带上去。修法：按**相对路径的每一段**判定，并且**每次改完立刻** `python3 tools/push-to-github.py --dry-run` 核对文件总数与清单 |
| **`grep` 的假阴性**（本机是 BSD grep） | 「查不到」某个符号，于是误判「代码里没有」 | BSD grep 的**基本正则不支持 `\|` 交替**，`grep -n "E6\|E7"` 会静默返回空。修法：一律用 `grep -E "E6|E7"` |
| **同一批改多个位置，有的没落地**（用 AI 编辑工具时） | 工具逐条报「成功」，但回读文件发现少改了一处、或者同一行写了两遍 | 一次消息里发多条编辑时，出现过被覆盖/丢失/重复（改 `push-to-github.py` 的排除规则时定义那半段丢了、改 AGENTS.md 目录地图时版本号没改上、坑表某行被写了两遍）。**2026-09-23 又踩一次**：同一个文件的两处改动**并发**发出，后一条按原文匹配、把前一条的改动整段覆盖回去（两条都报「Successfully edited」）。**修法：同一个文件必须串行改，一次只发一条编辑；改完回读文件核对，尤其批量编辑之后** |
| **「宽容」的解析器让界面撒谎** | 模型答了句闲聊、或写了一段没带标签的散文，它就以「设计说明总结」的身份显示在详情页与访客分享页；**不报错、看不出来** | `normalizeDigest()` 原本对收敛不了的输出**原样返回**，理由写在注释里：「不替模型编分类」。理由对、结论错——原样返回等于**界面断言这段是总结**。修法：不合规一律置空，原文留痕进 `ai_raw.digestRaw`（2026-09-22，构建记录 #030）。**教训：判「不收」的时候，宁可不显示，也不要把不可信的东西摆在一个断言它可信的位置上** |
| **版式问题「肉眼看着别扭」但功能性全对** | 搜索框在桌面只占 420px、右边 768px 全空；顶栏那个方块在窄屏是唯一剩下的东西。**不报错、断言不到、不查数据库**，所以最容易被当成「就是这样」 | 两处都是「只在某个宽度下才显形」的布局约束（一个 flex 行里两个 `flex:1` 抢空间；`.logo` 在 ≤460px 被 `font-size:0` 只剩方块）。修法：**先量几何、再改、改完按 1440 / 768 / 390 三档逐项断言**，判据写成等式（`搜索框宽 = bar 宽 − 计数宽 − 12px`）而不是阈值。三个测量陷阱：① **不等布局到最终态就量会拿到中间态**（同一处 202px vs 328px，要等 `#count` 有文字再量）；**而「`#count` 有文字」不等于最终态** —— 卡片是**两段式渲染**（先出文字、后补缩略图），第一段比最终态低 2.31px，会凭空产出 3 条「标签行顶端不齐」的假红（2026-09-23 实测，修法是把「缩略图数量与库一致」并进稳定判据）；② **元素还在 CSS 过渡里时量 `getBoundingClientRect`，量到的是一张在动的卡**（翻阅页牌堆在 `.34s` 内滑向正中，1420ms 采样点不同就差 36.29 / 11.66 / 6.54px，看起来像版式偏心）；③ **列表为空时 `main.list` 会按内容收缩**（≤800px 是 `align-items:flex-start` 的 column flex），量出来的宽度不作数。留证：`app/_verify_home_ui.html`（2026-09-22／2026-09-23，构建记录 #031／#044） |
| **入场时多出一段没人要的过渡** | 翻阅页每次打开，整叠牌都从右偏半张卡宽滑到居中（从详情页返回时同样滑）；验证台则报「中央主卡不居中」 | `.d-card` 的 CSS 默认 `transform` 是 `none`，而首帧 `layout()` 会设成 `translateX(calc(-50% + 0px))`，两者之差被判成一次过渡。**摘类必须同步收口**：第一版把 `classList.remove('booting')` 放进 `requestAnimationFrame`，实测只把偏移从 168px 减到 103px（**只减半**）—— 因为 `readGeom()` 里的 `getComputedStyle` 已强制过一次样式解析、那次 `transform` 还是 `none`，等下一帧摘类时过渡属性恢复就又被判成一次变更。改成 `void cardsBox.offsetHeight;` 紧接 `classList.remove('booting')` 后，三档宽度首帧偏移全为 0（2026-09-23，构建记录 #043） |
| **`backface-visibility:hidden` 只管「画不画」，不管命中测试** | 翻到背面后，背面右下角那个「详情」按钮**完全点不动**（点了反而翻回正面）；`elementFromPoint` 在按钮中心返回的是**正面**里的元素 | 两个面都是 `inset:0` 叠在一起，隐藏面只是不画，仍然参与命中测试。实测翻到背面后在按钮中心取样命中 `SPAN.dc-meta`（正面元素），于是点击从正面解析出 `.d-card`、退化成「切换翻面」。**修法：隐藏的那一面显式 `pointer-events:none`，只给当前可见面 `auto`**；并把「按钮中心必须命中它自己」写成回归断言（2026-09-23，构建记录 #046） |
| **异步数据回来把用户刚做的操作弹回去** | 「翻到背面」点了没反应，**再点一次又好了**；重开页面也复现不了 | 缩略图是 `cardsByIds()` 异步往返后才到的，`renderNode()` 一到就整体重画并**无条件复位翻面** → 用户刚翻开的背面被无声弹回正面。第一次没成功是因为图还在路上，第二次图已缓存所以「又好了」。**修法：重画只在「换素材」时复位**（`same = itemId 相同 && 已翻面`）；节点被回收给别的素材时 `itemId` 已被清零，`same` 必为 false，仍会回正面。**教训：任何「重画时顺手重置 UI 状态」的写法，都要先问一句「这次重画是同一份数据的补画，还是换了对象」**（2026-09-23，构建记录 #046） |
| **「鼠标按住能拖，触控板滑不动」** | 用户报 Mac 上双指左右滑翻不了卡；而按住鼠标拖是好的 | macOS 触控板双指左右滑发出的是 **`wheel` 事件（`deltaX`）**，不是 `pointer` 拖拽 —— 只监听 pointer 的那套代码永远收不到（实测连发 3 次 `deltaX=120`，队列一动不动）。**修法：桌面手势要同时覆盖 pointer 与 wheel**；纵向（`|dx| ≤ |dy|`）必须早退放行给页面与背面长文本，`{ passive:false }` + `preventDefault()` 顺手挡掉 Chrome 的双指前进／后退（2026-09-23，构建记录 #047）。**⚠️ 这条当时还写了「把 `deltaX` 折算进与拖拽同一套位移与阈值判据」——那半句是错的，已被 #049 推翻，见下一行。** |
| **滚轮不能当「一次甩动」，要当「连续滚动」** | 用户报「滑动不太跟手，而且两侧滑不过去」 | #047 把一趟滚轮手势当成**一次甩动**：`WHEEL_SCALE=.55`（打折跟手）＋ 位移被 `rubber()` **封顶在 `step*2`** ＋ 只在手势**结束时判一次方向**（最多翻一张）。实测跟手比只有 **0.31–0.55**（40px→22px、260px→106px、520px 被**封在 164px**且只翻 **1** 张）——520px 已超过 6 格，用户拿到的是「滑到一半就不动了」。**修法：位移 1:1（`WHEEL_SCALE=1`）、`⌊总位移 ÷ 步长⌋` 逐格当场翻页（不封顶）、只扣除真正翻过去的格数（余量跨事件累积）、不足一格余量空闲回弹、两端夹住即停；`rubber()` 退出滚轮路径，只留给真手指拖拽。** 改后跟手比 **1.00**、520px 翻 **4** 张。**教训：两条输入路径（指针／滚轮）该不该共用一套数字，要看它们的物理语义是否相同 —— 拖拽是「一次有始有终的甩」，滚轮是「连续的位移流」，强行共用会同时毁掉两边（2026-09-23，构建记录 #049）** |
| **`pointer-events` 的子元素会覆盖祖先的 `none`** | 用户报「点旁侧卡也会翻转，而且不会翻转回来」 | `layout()` 里写着 `pointerEvents = center ? 'auto' : 'none'`，本意是「只有中央卡可点」——**一次都没生效过**。卡片是个透明盒子，两个面（`.dc-front`/`.dc-back`）把它盖满且各自写着 `pointer-events:auto`，**子元素的 `auto` 会覆盖祖先的 `none`**，命中测试照样落在面的元素上。于是侧卡的点击一路走到 click 处理器最后那句 `setFlipped`，「点侧卡也翻面」；而侧卡翻面后会被布局重新摆回侧位，看起来「翻不回来」。**修法不是给侧卡补 `none`**（那会把用户要的「点侧卡跳过去」一起废掉），而是删掉那句假防线、在 click 处理器里用 `isCenter` 分流：侧卡走 `goTo(qi)`（滚过去，不翻它），翻面只发生在中央卡上。**教训：「谁可点」不能只看卡片这一层，要看最终命中测试落在谁身上 —— 设了 `pointer-events:none` 却在子元素上又放开，等于没设（2026-09-23，构建记录 #048）** |
| **给「先复位再翻页」这类规则留了多个入口** | 每多一条导航路就漏一次复位（拖拽修好了、按钮修好了、点侧卡又漏） | #046 把复位写在 `commit()` 里，而 `commit()` 只覆盖拖拽与「上一张／下一张」两条路；#048 新增「点侧卡」第三条路时立刻漏掉。**修法：收进唯一出入口 `goTo(ni)`，`commit(dir)` 退化成 `goTo(d.index + dir)`，所有导航路都必须过它。** 判据：静态断言里 `setFlipped(..., false, true)` 只出现在 `goTo` 内（2026-09-23，构建记录 #048） |
| **合成事件测试不出「手势」的真相** | 用脚本连发 3 次滚轮，验证台连着给出三种假结论 | ① **「一次手势」= 若干条间隔远小于空闲判据的事件**：分开等 320ms 发 3 条会被判成 3 次独立手势（连翻 3 页），与真实双指滑不符 —— 应批成 40ms 间隔 + 末尾 300ms 收尾。② **「格数」必须按当前档位的真实 `--deck-step` 反算**，写死像素值会随档位错（桌面 `step=82`、手机 `step=44`）；**要量「跟手」就量卡片实际渲染位移**（`getBoundingClientRect` 的 `cx`，手势期间 `.dragging` 关掉了过渡、rect 是即时的），**不要读内部变量** —— `Elangit.projects.__deck()` 是**裁剪过的快照**，没有 `dragX`，读了会得到 `undefined`/`NaN` 的假红。③ 浏览器双指手势的**默认行为**只有 trusted 事件才有，合成 `WheelEvent` 覆盖不到 —— 那一半要用 CDP `Input.dispatchMouseEvent type:mouseWheel` 打真实事件（2026-09-23，构建记录 #046／#047／#049） |
| **手机端「卡片太小 / 手感很钝」——两个都是数值没量过** | 390 屏上主卡只有 228×328（占屏宽 58%）、纵向空着 300 多像素；手指横移约 5mm 卡片就顶着不动 | ① 尺寸：`--deck-cw/-ch` 在 ≤640 档是拍的不是量的，而且**同一档还留着为 228px 卡身写的内文紧缩样式**，放大卡宽后会出现「卡变大了、字还是小的」——两条要一起改。放大时**高度不能按宽度的比例走**：`--deck-ch` 是固定值而 deckbox 高度随视口收缩，撑满 390×844 的尺寸到 320×568 上就顶出 deckbox（余量只剩 10px）。② 手感：`onMove` 把跟手位移硬截在 `step×1.15`（手机档 `step=44` → 52.9px）。改法不是取消上限（原意「别让卡片被甩出屏幕」要保留），而是换成**在 `free` 处连续的分段阻尼** `rubber(dx, free, k, cap)`，取 `free=step, k=.4, cap=step*2`。实测手指 +30/+70/+150px → 卡片跟手 30.0/54.4/86.4px，旧代码三处都是 50.6px（2026-09-23，构建记录 #045）。**这两条都是「用户真机一句话」暴露出来的，验证台当时 84 条全绿——说明断言只覆盖了「有没有坏」，没覆盖「够不够大 / 够不够跟手」** |

---

## 8. 不要做的事（用户明确定的边界，写在这一节就是要你顶住）

1. **不要加跨平台自动抓取**（小红书/公众号）。技术与合规上都有硬限制，第一版奔着它去会卡死。**用户可能会反复提，要顶住。**
2. **不要加账号体系 / 多用户。** 第一版只有一个用户。数据结构已预留 `user_id`——「预留接口」和「实现功能」是两件事，要讲清楚。
3. **不要把录入路径做长。** 单条 ≤30 秒是这类工具的最大死因：灵感出现时用户在刷手机，操作一麻烦，用两周就放弃。AI 处理**不计入阻塞时长**（本地 ≤1s 出图入库，AI 异步补齐，90s 超时降级为手填）。
4. **不要只靠 AI 自由标签。** 标签必须优先复用已有标签库（提示词里带枚举），抽屉是**封闭集合**、AI 只能从中选一个。前 50 条要手动复盘一次。
5. **不要做成「另一个收藏夹」。** 任何功能决策都要回到「这样能让我更快**找回**吗」。
6. **不要扩大范围。** 用户容易在讨论中加想法，每次都问「这属于第一版吗」。
7. **不要在没验证的情况下宣称「已验证」。** 用户上一个项目（AI 求职助手）的教训就是从头到尾没做过一次真实验证。这个项目必须真的用起来、并留下数据。
8. **不要把 iCloud 云盘当产品数据层。** 网页应用读不了它。

---

## 9. 交付纪律（用户在这个项目上反复强化的）

- **每条结论挂证据**：实测数字 + 对照基准。没有基准的「看起来是对的」不算结论。
- **数值类结论要多轮**：同一张图 AI 给的主体矩形宽度实测在 59%~70% 之间抖动（±10 个百分点），单次测量不能当结论。**这是已知限制，不是待修 bug。**
- **如实记限制**，并且写在显眼处：前端口令不是安全边界；埋点是前端记录不是可信审计；E10 不做所以拿不到「分享实际价值」的量化。
- **改完文档要同步**：`docs/构建记录.md` 追加一条（背景 / 问题 / 回答或决策 / 理由 / 影响文件 / 下一步），`docs/04-个人灵感库-PRD.md` 与 `docs/06-技术方案.md` 若口径变了要升版本并写明变更依据。
- **注意入口**：`app/_verify_*.html` 是**一次性验证台**（结论落地前不要删，但它们不属于产品代码，改产物时不要动它们）。
  - 现有五个：`_verify_ai.html`（AI 输出契约，23 条）、`_verify_home_ui.html`（首页/顶栏/卡片版式，**118 条**，覆盖 5 个页面 × 3 档宽度）、`_verify_stats.html` + `_verify_stats_ui.html`（判定口径与数据页）、`_verify_projects.html`（项目筛选一期，**157 条**：源码边界 + 四档牌堆几何（1440/768/390/**320**）+ 四种真机尺寸的矮屏竖向 + **卡片翻面（背面命中测试）／点旁侧卡不翻面／触控板连续滚动**的行为断言 + 真实云库写入往返，**自建临时项目并自清理**）。**动了顶栏、`.bar`、`.search` 或它们的 CSS，就重开 `_verify_home_ui.html` 跑一遍** —— 它同时盯着 5 个页面在 1440 / 768 / 390 下有没有被挤坏；**动了 `app/projects/` 或 `store.js` 的项目相关方法，跑 `_verify_projects.html`**。
  - **翻面／手势这类交互要「行为断言 + 真实事件」两层**：验证台里合成 `WheelEvent`／`MouseEvent` 能覆盖代码路径（页面不检查 `isTrusted`），但覆盖不到浏览器手势的**默认行为**与 `preventDefault` 的真实效果——那一半要用 CDP 打 trusted 事件（`Input.dispatchMouseEvent type:mouseWheel`）。另外 CLI 的 `mouse wheel <dy> [dx]` **不带坐标**（固定发在 0,0），测「鼠标停在牌堆上滚动」必须走 CDP。
  - **量「有没有被裁切」必须用真机尺寸，不能用固定高度的 iframe。** `_verify_projects.html` 里那四个宽度档的 iframe 高度固定 900px；在那个高度里任何卡都装得下，「不被裁切」是白送的。而翻阅页的 `--deck-ch` 是**固定值**、deckbox 高度**随视口高度收缩**，所以风险只在矮屏 —— 实测余量在 390×844 是上下各 116px，到 390×667 只剩 27px、320×568 只剩 10px。该验证台因此单列一组「四之二、矮屏竖向」。同理：**CSS 里的数值断言（`@media` 里写了什么）替代不了真实 `getBoundingClientRect`** —— 前者只能证明「我写了」，`overflow:hidden` 的裁切、被遮挡、落到视口外这些都要量。
  - **读结果别读错元素**：`_verify_projects.html` 的结论在 `#verdict`，`_verify_home_ui.html` 在 `#sum`（没有 `#verdict`）。2026-09-23 用脚本轮询 `#verdict` 等首页回归，等了 7 分钟才发现是元素名写错、测试其实早就跑完了——**轮询前先确认结论元素的 id**。想逐条读结果时，`_verify_home_ui.html` 另有 `window.__verify = { total, pass, fail, skip, rows }`（比解析 DOM 可靠）；`_verify_projects.html` 只能读 DOM（失败行是 `tr.f`、跳过行是 `tr.s`），另有 **`document.body[data-verify-done]`** 作为「整轮是否走到收尾」的机器可读标记。
  - **⚠️「没有结论」不是一种结论 —— 验证台自己也会静默死掉。** 2026-09-23 实测：表格停在 142 行、`#note` 空白、汇总永远不出来，**看上去像「还在跑」，其实异步函数已经中断**。两个诱因都记在这里：① 断言读了 `__deck()` 裁剪快照里不存在的字段（`nodes[].el`）→ 抛错中断；② 为「抛异常也留痕迹」加的 `try` 起点写在了 `'use strict'` 之后 → **严格模式下块内函数声明是块作用域**，`finish`/`ok`/`push` 全被关进 `try` 块，收尾报 `finish is not defined`（表象与①一模一样）。**修法：整轮的 `try` 起点必须放在所有函数声明之后**（`loadSrc()` 定义完再开），catch 里 `push` 一条红色断言并照常收尾，收尾再包一层 `try`，末尾写 `data-verify-done`；并加一条**正向断言「整轮跑完并到达收尾」** —— 只靠「失败数为 0」判断不了「跑到一半就死了」。**判断验证台是否跑完，看 `data-verify-done` 或那条正向断言，不要看失败数。**
  - **收尾提示也要如实**：`_verify_projects.html` 原来把「建了临时项目又删掉」和「根本没建」都报成「未创建」（`tmpId` 已在收尾清零）。**报「已完成」的话必须是能验证的**，否则下一个接手的人会以为库里干净。
  - **测试残留当场清，不能只写在报告里。** 上一次卡死的运行在库里留下过一条 `#16 【验证台】临时项目`（14:12 创建），是下轮排查时才发现的——它会让后续每一轮的「无残留」断言变红、并把基线抬高。**每轮收尾固定回查：`projects` / `project_items` / `items` 三张表的行数，以及有没有 `【验证台】` 前缀的项目。**
  - **轮询要有上限且别让命令超时**：验证台一轮要 40～90 秒（要建临时项目、跨多个 iframe）。用 `for i in $(seq …)` + `sleep 6` 轮询时，整条命令容易被前台超时掐掉（`Exit Code: 137`）——**掐掉的是轮询命令，不是页面**，重开一次读结果即可，不必重跑验证台。
  - **「跳过」不等于通过**：每条跳过都要写明原因（例如「该页列表受 token 门控、DOM 里 0 张卡片」），不能静默略过。
  - **必须经 http 打开**（本地 `python3 _serve.py`）。`file://` 下浏览器禁止读 iframe 内部 DOM、也不允许 `fetch` 同目录文件，那样所有断言都会红 —— 那是环境问题，不是页面问题。
- **版式类改动要留截图**：`app/_verify_shots/`（该目录名与 `_verify_` 前缀都在两条流水线的排除规则里）。

---

## 10. 敏感信息提醒

本目录同时是 **Codex 的工作区**（要真值）和 **公开仓库的上游**（不能带真值）。两者的差异由 `tools/push-to-github.py` 在上传时处理，**不要手工同步、不要在本地 `git push`**。

**上传时被替换掉的（磁盘上仍是真值）**：

| 内容 | 公开仓库里的样子 |
|---|---|
| `app/config.js` · `publishableKey` | `wbpk_<你的 publishableKey>` |
| `app/config.js` · `ownerPasscodeHash` | `''` |
| `data/tools/export-all.html` · `publishableKey` | `wbpk_<你的 publishableKey>` |
| `docs/09-简历…md` · 手机号 / 邮箱 | `【你的手机号】` / `【你的邮箱】` |

**上传时被整个排除的**：`docs/07-个人基础资料（私密）.md`、`data/dump.sql`、`data/items.json`、`data/elangit-dump.json`、`data/images/`、`data/originals/`，以及根目录的**操作说明与交接稿**类文档（`如何推送.md`、`如何把项目交给Codex.md`、`giveworkbuddy.md`、`统计口径修正-方案（待确认）.md`，还有按 **`交接给` 前缀**命名的每轮交接稿——`push-to-github.py` 的 `EXCLUDE_PATH_PREFIX` 已含该前缀，**下一轮换日期不用再改排除名单**）。

⚠️ **别把「排除」当成 `.gitignore` 在起作用**：`Elan git/` 这份 `.gitignore` 只是政策说明（本目录**不是 git 仓库**），真正决定什么进公开仓库的是 `tools/push-to-github.py` 里的 `EXCLUDE_*` 名单——它不读 `.gitignore`。改完排除规则**必须实跑一遍** `--export-to` 看导出目录，别只看代码。

**为什么必须这么做**：

- 数据库 9 张表的 RLS 策略全是 `USING(true)`，且已 GRANT 给 `anon`。也就是说，**拿到 `endpoint` + `publishableKey` 的人可以直接读写全库**（不是只读）。
- 有一个缓解因素：服务端强制 Origin 精确匹配，纯脚本调用需要伪造 `Origin` 才能过。但这是减速带，不是墙——**能伪造**。
- **⚠️ 2026-09-20 起，key 确实在公网上了**（任务 9 已发布）。发布件必须保留真 key：纯前端直连库，且分享链接要让**收到链接的人**也读得到数据，所以本架构下不存在「只有所有者持有 key」。**别把「公开仓库已脱敏」误读成「key 是秘密」**——脱敏只是不让仓库成为第二个泄露点。
- 由此两条后果：**撤销发布不收回 key**（先前拿到页面的人手里那份仍然可用，真要收回只能轮换 key）；**云库不能当唯一副本**（key 一旦被滥用，最坏情况是全库被清，所以素材值钱起来后要定期导出，用 `data/tools/export-all.html`）。

因此 `publishableKey` 上传前一律置成占位符。带来的后果是**从 GitHub clone 下来的那份跑不起来**，这是预期的：要跑就用磁盘上这份，或向用户索取凭据。

- `docs/07-个人基础资料（私密）.md` 是用户的私人素材（个人经历、简历原始材料），**任何时候都不要提交**。
- 若将来用户希望公开仓库里的演示能直接跑通，**不要直接把真 key 塞回 config.js**——正确做法是给公开版本单独建一个演示库（新的 app + 新的 key），两边数据隔离后再展示。
