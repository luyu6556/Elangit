# 首次真实录入测试 · 问题清单

> 2026-09-20 深夜测，2026-09-21 落地。用你在 iCloud《灵感库测试/测试.txt》里攒的 **16 个 gooood 链接**做了一次真实录入 + 测试。
> 每条问题都带「现象 / 证据 / 根因（文件:行）/ 影响 / 建议修法」。**结论已落地**，见文末「决策与落地」与 `docs/构建记录.md` #025。

---

## 决策与落地（2026-09-21）

你的四项拍板，均已执行：

| # | 决策 | 落地结果 |
|---|---|---|
| ① 测试数据处置 | 16 条随机保留 3 条（id 8/11/15）用于后续测试 | 已删 13 条 + 4 条修 bug 时的残留，库回到 6 条（原 3 + 保留 3） |
| ② P0 两条口径错 | 现在修 | P0-1 A1 计时起点改「本条第一个输入动作」；P0-2 A2 分母改「AI 实际产出非空的样本」 |
| ③ P1-1 存网址没封面 | 选 **B**（加服务端抓原文标题/首图） | 新增 `app/_page_meta.py` + `_serve.py` 挂 `/api/meta`、`/api/img`，线上已跑通 |
| ④ 问题清单进公开仓库 | 需要 | 本文件转正为公开版，作为作品集素材（「第一次做真实用户测试，找出 8 个问题」） |

落地明细与「为什么」见 `docs/构建记录.md` #025、`docs/04-个人灵感库-PRD.md` v12、`docs/06-技术方案.md` v12。

---

## 0. 怎么测的（先交代测试手段，免得你看不懂数据来源）

- **在线上环境测**，不是本地：`https://elangit.app.workbuddy.host/`（任务 9 发布的那个）。
- **我用了一点非常规手段，必须说明**：所有者门禁是纯前端的，我没有你的口令，所以我在浏览器里直接把解锁标记写成已解锁（`localStorage['elangit.owner'] = '1'`），再用真实界面一条条录。**这没有绕过任何服务端校验**（本来就没有服务端校验），但它意味着「输入口令解锁」这条路径这次没有被测试——那条路径在 #017 已经单独验过。
- 录入方式：**完全按真实操作**——只填「网址」一栏，「来源平台」保持默认的「让 AI 判断」，然后点「收进灵感库」。文本、备注、截图都不填（因为你的输入就是 16 个链接）。
- 16 条**一次性连录**（模拟「刷完一圈一起收」），而不是一次开页面录一条。
- 测试前的库：3 条素材。测试后：19 条。**测试数据现在还在库里，没有清理**（见文末「要你拍板的」）。

**录入结果概览**

| 指标 | 结果 |
|---|---|
| 提交本身 | 16/16 成功，**0 报错**，每次提交实际耗时 **200–402ms**（16 次全部 ≤402ms） |
| AI 识别 | 首次 **15/16 成功**，1 条失败；点「重跑 AI」后成功 → **最终 16/16 完成** |
| AI 总耗时 | 16 次串行跑完约 **5 分钟**（平均约 19 秒/次，与之前实测的 3–58 秒方差一致） |
| 标题质量 | 出乎意料地好：AI 完全靠 URL 里的英文 slug 猜，16 条里至少 12 条猜对（衡水垂直农场 / 松江泗泾体育中心 / 青山自然学校 / 虹口砖砌加油站改造…） |
| 封面 | **16 条全部没有封面**（`cover_source = none`）——这条是本次最重要的发现，见 P1-1 |

**16 条明细**（标题 / 抽屉 / 标签）

| id | 标题（AI） | 抽屉 | 标签 |
|---|---|---|---|
| 8 | 2025阿卡汗建筑奖得主 | 其他 / 待归类 | 案例参考、竞赛参考、文化建筑、社区营造 |
| 9 | 衡水垂直农场 | 建筑与构筑物 | 案例参考、产业建筑、低碳技术、水资源循环、气候适应 |
| 10 | Ghaf森林体验中心 | 建筑与构筑物 | 案例参考、商业、住宅、材质、光影 |
| 11 | 坡上庇护所：SS花园 | 建筑与构筑物 | 构筑物、景观装置、社区营造、公园、案例参考 |
| 12 | Qinglu | 其他 / 待归类 | 案例参考、**待核实**、**网页收藏** |
| 13 | 解忧小屋 | 其他 / 待归类 ⚠ | 案例参考、社区营造、构筑物 |
| 14 | Bull竹构烹饪教学亭 | 建筑与构筑物 | 案例参考、构筑物、材质、低碳技术、低成本建造 |
| 15 | 松江泗泾体育中心 | 建筑与构筑物 | 案例参考、体育建筑、公共建筑、社区营造 |
| 16 | 虹口砖砌加油站改造 | 建筑与构筑物 | 案例参考、旧建筑改造、材质、构筑物 |
| 17 | 青山自然学校 | 建筑与构筑物 | 学校、旧建筑改造、社区营造、案例参考 |
| 18 | 欢乐社区 | 建筑与构筑物 | 住宅、适老化、社区营造、案例参考 |
| 19 | 南大幼儿园改造 | 建筑与构筑物 | 旧建筑改造、学校、儿童友好、颜色搭配 |
| 20 | 帕扎洛住宅 | 建筑与构筑物 | 住宅、案例参考、瑞士建筑 |
| 21 | Villa Lavan | 建筑与构筑物 | 案例参考、住宅、极简 |
| 22 | Wiedenhofer | 其他 / 待归类 ⚠ | 案例参考、旧建筑改造、住宅 |
| 23 | 实心石材住宅单元 | 建筑与构筑物 | 住宅、材质、案例参考 |

⚠ 那两条标了的是我认为判错的（见 P1-3）。

---

## 1. P0 — 两个指标口径错，会给出「看起来有依据」的假数字

### P0-1　A1「单条录入平均 ≤ 30 秒」在多条会话下系统性虚高

**现象**：我连录 16 条，A1 采集到的 16 个耗时是 **27.8 秒 → 45.5 秒，单调递增，每条 +1.2 秒**。均值 **36.6 秒**，**16 条全部超 30 秒**。

**反证**：我实测每次提交实际只花 **200–402 毫秒**。两者差了两个数量级。

**根因**：`app/add.html:136` 定义了 `pageOpenedAt = Date.now()`（脚本初始化那一刻），E5 事件记的是 `Date.now() - pageOpenedAt`（`app/add.html:366`）。
→ **这个数字是「从打开录入页到这一条入库」的累计值**，不是「这一条的录入耗时」。一次会话录第 N 条，把前 N−1 条的处理时间和中间的浏览时间全算进去了。

代码注释里写得很清楚：「A1 判据：从打开录入页到入库 ≤ 30 秒」。**问题在于这个定义和 PRD/数据页的文案「单条录入平均 ≤ 30 秒」不是一回事。**

**影响**：
1. 现在 A1 样本 16/20、显示「样本不足」。**只要再录 4 条（正常使用，一天就能到），数据页就会显示「A1 不达标（均值 36.6 秒）」**——而这个不合格完全是我这次批量录入造成的假象。
2. 「打开一次录入页连录几条」是最自然的用法，所以这不是测试特例，是常态。

**附带一个更麻烦的**：**删素材不会删事件**。E5 存在 `events` 表里，`loadFirstEvents('item_create_finish', 20)` 取的是「最早 20 条事件」。所以**就算把这 16 条素材全删掉，A1 的 20 条样本窗口里仍然留着这 16 条**。要清干净必须连 `events` 一起清。

**建议修法**（待你定，有取舍）：
- 方案 a：把起点改成「本条的第一个输入动作」（在 `#fText`/`#fUrl`/`#file` 上有输入就重置起点；没有输入动作时退回页面打开时刻）。语义最接近「录这一条花了多久」。
- 方案 b：起点改成「上一条提交成功之后」（`clearForm()` 那一刻）。实现最省事，但用户在想别的事时也会被计入。
- 方案 c：不动实现，改文案，把这个指标明确定义成「一次会话里录到第几条的累计耗时」，并在数据页拆成「单条耗时」和「会话累计」两个数。**但我认为这不是好选择——A1 问的就是「灵感出现时能不能 30 秒内收进来」。**

---

### P0-2　A2 的逐字段可用率会把「AI 什么都没产出」算成「可用」

**现象**：16 条有原值样本，A2 的**每一个字段**可用率都是 **100%**，包括：`项目名称 100% (16)`、`摘要 100% (16)`、**`画面描述 100% (16)`**、**`图中文字 100% (16)`**、`抽屉 100% (16)`、`来源平台 100% (16)`、`标签 100% (16)`。

**问题**：这 16 条里**有图的一条都没有**，AI 的 `caption`（画面描述）和 `ocr_text`（图中文字）**全部是空字符串**——它压根没产出这两个字段。可是指标显示它们「100% 可用」。

**根因**：`app/diff.js:43` 的判定是 `f.changed = f.before !== f.after`，即「AI 原值 ≠ 现值 才算改过」。
- 没有图 → AI 给 `caption: ""`，库里现值也是 `""` → `changed = false` → 计入「没被改过」→ 算进「可用率」的分子。
- **于是「AI 什么都没给」被读成了「AI 给的东西没被改过，所以可用」。**

**影响**：等 A2 样本攒到 50，只要样本里混着「无图素材」，这两个字段就会永远显示接近 100% 的可用率。这是一个**会自我实现的假通过**——数字越漂亮，越没人去查。

**建议修法**：分母从「有原值的素材数」改成「AI 在该字段上**实际产出非空**的样本数」。产出为空时，这一条不进这个字段的分母；如果某个字段的「产出非空」样本为 0，就按「样本不足」处理，不给百分比。

**这条和 #023 修的是同一类问题**（口径/分母），但方向相反：#023 那条是分母被做小、成功率被高估；这条是**分子被虚增**。

---

## 2. P1 — 产品与真实用法不匹配

### P1-1　存网址这条路，产出的是一屏没有图的灰块

**现象**：16 条新素材**全部没有封面**（`cover_source = none`）。首页 19 条里 **17 条**是灰色的「这条没有配图 / 纯文本素材照样进库」占位块。整页看上去像加载失败。

**这是产品问题不是 bug**：卡片和详情页都渲染了「原文」链接（`app/cards.js:189`、`app/item.html:316`），点得开原帖，所以不是死数据。但**产品从没尝试去抓网页的标题或首图**——AI 拿到的只有那一串 URL，靠 slug 里的英文词猜。

**为什么这条最重要**：v1 的采集方式定的是「截图 + 文本复制」（PRD F1），可**你的真实习惯是存链接**——这份测试文件本身就是证据（16 条全是链接，一条截图都没有）。这两件事不匹配。

**取舍**（要你定，成本差别很大）：
- **A. 接受边界**：在录入页把「只填网址」明确标成「会入库但没有封面」，或者干脆要求「存链接时顺手截一张图」。零开发成本。
- **B. 抓网页的标题和首图**：需要服务端（浏览器的跨域限制拿不到 gooood 的 HTML），而现在整个应用是纯前端 + 云 SDK，没有服务端。这是**架构级改动**，不是加个功能。
- **C. 折中**：录入时把 URL 交给 AI 做个「一次联网检索」（如果云服务的模型支持联网的话）。需要先验证云服务有没有这个能力。

我的看法：**先做 A，把「无图」这件事说清楚**；B 值不值得做，取决于你实际攒到 30 条之后，有多少是链接、有多少是截图。

---

### P1-2　AI 失败后的「重跑 AI」入口会过期

**现象**：16 条里 1 条（#16 虹口加油站）首次 AI 失败（约 6%）。我点了它那行的「重跑 AI」，**成功了**（标题、抽屉、标签都正确）。但——

1. **重跑入口只在录入页的「最近 8 条」列表里**。这条恰好是第 8 条所以在；**再录 1 条它就掉出窗口，那条就永远没有重跑的入口了。**
2. **失败原因不落库**。`app/add.html:532` 的注释自己写了这件事：「失败原因只存在内存里，刷新页面后就不知道是超时还是报错了」。所以刷新之后，只知道「失败了」，不知道是超时、是模型返回了非 JSON、还是网络问题。
3. **列表页看不出失败**。首页卡片上完全没有任何失败标记，那条失败的显示成「未命名 / （没有文字内容）」，和「AI 判不出名字」长得一模一样。只有点进详情页才会看到「AI 识别没完成 · 可在录入页重跑」。

**建议修法**：
- 把失败原因写进库里（`items` 加一列，或用 `ai_raw` 旁边加 `ai_error`），至少存「超时/其他 + 时间」。
- 录入页的「最近 8 条」加一个筛选或入口，能列出「所有失败待重跑」的素材；或者详情页直接给「重跑 AI」按钮（现在只有录入页有）。
- 卡片上加一个失败标记。

---

### P1-3　兜底抽屉 21.1%（4/19），但横幅把原因归错了

**现象**：首页横幅显示「有 4 / 19 条落在「其他 / 待归类」（超过 10%）。该回头调整抽屉划分了 —— PRD A13 的复盘信号。」

**但看具体是哪 4 条**：

| 素材 | 落兜底的原因 | 是不是「抽屉划分不对」 |
|---|---|---|
| 2025阿卡汗建筑奖得主 | 这是一个**奖项榜单页**，不是单项目，本来就没有归属 | 不是 |
| Qinglu | AI 不认得这个词（实际是 JULY PARTNERS 的项目），明确说了「待核实」 | 不是，是**判不出** |
| 解忧小屋 | **判错了**，一个小屋应该归「建筑与构筑物」 | 是，但属于 AI 判断失误，不是抽屉定义问题 |
| Wiedenhofer | **判错了**，旧墙置入新空间，至少该进「建筑与构筑物」 | 同上 |

**结论**：4 条里 2 条是「AI 判不出/判错」，1 条是「输入本身不该归抽屉」，**没有一条是「抽屉定义有缺口」**。但横幅一律说「该调整抽屉划分了」，会把归因引到错的方向——按它的字面意思去改抽屉，是白改。

**建议修法**：横幅文案区分两种信号——「AI 判不出」（提示词/模型问题，或该素材类型不该期望被自动归类）vs「落点分散在多个抽屉」（这才是抽屉定义问题）。或者至少在文案里补一句「先看是判不出还是判错」。

---

## 3. P2 — 会被记住但优先级低

### P2-1　标签体系被稀释
- **`案例参考` 出现在 18/19 条（95%）**，作为筛选维度几乎没有区分度——点它和点「全部」差不多。
- AI 自造了**元信息型标签**：`待核实`、`网页收藏`。这两个不是内容标签，是「我对这条不确定 / 这是网页」的元信息，进库后进了「未分组（AI 新造）」，会一直留着。
- 建议：给提示词加一条硬约束——「不要产出表示不确定或来源类型的标签」；`案例参考` 这种高频标签考虑改成不进标签、只在抽屉里体现。

### P2-2　`source_type` 语义错
只填网址、不填图不填文本时，`source_type` 被算成 `'text'`（`app/add.html:322-323` 的三元表达式只区分了 image / mixed / text）。
一条「只有链接」的素材既不是 image 也不是 text。建议加 `'link'`（或至少 `'mixed'`），否则以后想按录入方式做统计分析会失真。

---

## 4. 通过的部分（这次也验掉了几件事）

- **E14 检索结算在线上真的跑通了**（#023 的修正生效，这是第一次在真实环境验证）：
  搜「加油站」→ 命中 1 条（虹口砖砌加油站改造）；E6 `search_execute`、E7 `search_result_click`、E14 `search_attempt_end` 三个事件落在同一个 `search_id` 上（`mu9zxvof-9jyr-1`），E14 记为 `outcome: timeout`。
  （说明：那次测出来 11.2 秒是因为我自动化点击之间有等待，人工不会那么慢——**这不是产品问题**，反而说明计时口径是对的。）
- **按项目名和原文搜索有效**：搜「加油站」精确命中 1 条，没有误召回。
- **门槛保护正常**：数据页 A1(16/20)、A2(16/50)、A4(1/10)、A12(1/14)、A13(19/30) 全部正确显示「样本不足」，**没有出现任何不该出现的百分比**。
- **AI 失败不丢素材**成立：失败的那条素材在库里，重跑后补齐，没有产生重复条目。
- **提交路径很稳**：16 次提交 0 报错，200–402ms 入库。

---

## 5. 待你拍板（已闭环）

以下四项已于 2026-09-21 全部拍板并落地，结论见文首「决策与落地」表。保留原文供回看当时的选项与我的倾向。

**① 这 16 条测试数据怎么处理？**
→ **定**：随机保留 3 条（id 8/11/15）用于后续测试，删 13 条 + 4 条修 bug 时的残留。

**② P0 的两条口径错，现在修还是先放着？**
→ **定**：现在修（P0-1、P0-2 均已落地）。

**③ P1-1（存网址没有封面）你倾向 A / B / C 哪条？**
→ **定**：选 B（加服务端抓原文标题/首图，架构级改动，已上线）。

**④ 这份清单要不要进公开仓库？**
→ **定**：需要。本文件转正为公开版，作为作品集素材。
