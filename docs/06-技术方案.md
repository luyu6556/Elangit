# 个人灵感库（Elangit）· 技术方案

> **阶段**：阶段三 · 设计与研发评审（**v13**，随阶段四实装进度更新）
> **生成日期**：2026-09-19（v2 修订 2026-09-20，v13 修订 2026-09-22）
> **上游**：`docs/04-个人灵感库-PRD.md`（v13）
> **配套界面稿**：`docs/05-界面稿.html`
> **下游**：阶段四 研发（**任务 1~9 全部完成，应用已上线**：https://elangit.app.workbuddy.host/ ，进度见第 9 节）

---

## 1. 架构总览

```
   手机浏览器 ──┐
                ├──→ 线上网页（单页应用，响应式）
   电脑浏览器 ──┤            │
                │            ├─ 数据库：素材 / 抽屉 / 标签 / 分组 / 设置
   分享链接访客 ─┘            ├─ 文件存储：截图与上传图片
                              └─ 大模型：摘要 · 画面描述 · 图中文字 · 标签 · 分类
```

**一句话**：一个部署在云端的响应式网页应用，数据与 AI 能力全部使用 WorkBuddy 云服务，用户不需要买服务器、不需要域名、不需要自备 API Key。

**明确不搭的东西**：不做自建后端服务，不做用户注册登录体系，不做原生 App。

---

## 2. 技术选型

| 层 | 选型 | 理由 |
|---|---|---|
| 前端 | 单文件 HTML + 原生 JS + 内联 CSS，无构建步骤 | 项目规模小（4 个页面）；无构建负担意味着你以后想改文案不用装环境。**实测前提是包体控制在单文件可维护范围内**，若后期代码超过 ~2000 行再考虑引入构建 |
| 存储 | 云服务 **Database** 模块 | 跨设备同步与分享的前提（PRD Q-A 已确认必须上云） |
| 图片 | **不使用云服务 Storage 模块**；图片统一转 1600px 后以 base64 存入数据库 `item_images` 表 | **2026-09-20 修订**。实测平台 Storage 只对已登录用户开放（连读都要登录），未登录访客读不到图；而 `Auth` 又明确不支持匿名登录。改用数据库后，未登录访客也能看到图片，这与「不做账号体系 + 只读分享给外人」两条是兼容的。代价与体积实测见 3.6 |
| AI | 云服务 **LLM API** 模块（免密钥） | PRD Q-H 已确认先用平台自带能力。**多模态已验证通过**（见 4.2） |
| 账号 | **完全不启用**云服务 Auth 模块 | PRD 不做账号体系。所有者保护退化为纯前端口令（见第 6 章） |
| 部署 | 发布为在线应用，复用同一应用 ID | 应用 ID 复用才能保住域名与云服务校验的匹配关系 |

**SDK 引入方式**：项目是纯 HTML、无构建步骤，按规范走 **CDN 引入**（不使用 npm 安装），初始化时同时传入 `endpoint` 与 `publishableKey` 两个公开配置值。这两个值在激活云服务后由平台返回，**不写死在代码里**。

---

## 3. 数据表设计

**2026-09-20 修订**：图片从 Storage 改为 base64 存入数据库，表数由 5 张变为 **6 张**。本节的表结构**已实际建成并验证通过**（状态见 3.7）。

### 3.1 items（素材，主表）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | 自增整数 | 主键 |
| user_id | 字符串 | **固定值 `local-owner`，预留**。为将来接账号体系留的接口，第一版不参与筛选 |
| source_type | 字符串 | `text` / `image` / `mixed` / **`link`**（2026-09-21 新增：只填网址、无图无文本时，既不是 image 也不是 text） |
| raw_text | 文本 | 粘贴的原文 |
| source_platform | 字符串 | 小红书 / 公众号 / 网页 / 其他 |
| source_url | 字符串 | 原文链接 |
| **page_title** | 文本 | **只填网址时服务端抓来的原文标题**（2026-09-21 新增，见 9.8 第 9 条）。落库是为了重跑 AI 时复用（AI 排在入库后异步跑，刷新后 pending 条目重排队时内存里已无抓取结果），也进轻量索引让「搜工作室名/项目原名」有效 |
| **page_desc** | 文本 | 服务端抓来的原文描述，同供 AI 分类与重跑复用。不进索引（信息密度被 `ai_summary` 覆盖） |
| **page_text** | 文本 | **服务端抓来的网页正文**（2026-09-22 新增，见 4.4）。去导航/操作位噪声、双语站去掉英文半篇，上限 **8000 字**。**只喂 AI，不进索引**——它比 `page_desc` 长两个量级，进索引会让列表页每次查询白拉几十 KB |
| ai_summary | 文本 | AI 一句话摘要（≤50 字）。**首页卡片只用这一句** |
| **ai_digest** | 文本 | **设计说明总结**：三行固定格式（理念 / 做法 / 效果，≤120 字，2026-09-22 新增，见 4.4）。**只在详情页展示**；判定不是设计类项目则为空串。`ai_raw` 里对应 `digest` 键 |
| **ai_title** | 文本 | **案例/项目名**，卡片与详情页首个可见字段（任务 6 补，见下） |
| ai_caption | 文本 | AI 画面描述 |
| ocr_text | 文本 | AI 提取的图中文字 |
| ai_tags | 数组 | AI 生成的标签名列表 |
| my_tags | 数组 | 用户手动加的标签名列表 |
| category | 字符串 | 抽屉名（**必须来自 categories，AI 无权新建**） |
| **cover_thumb** | 文本 | **卡片缩略图（base64）**。长边 400px / JPEG q72，列表页只取这个字段 |
| **cover_source** | 字符串 | 封面来源：`crop`（按 AI 矩形裁取）/ `raw`（整图未裁）/ `none`（这条没有图）/ **`link`（取自原文网页，库里无原件，2026-09-21 新增）**。**取值与代码一致，本行 2026-09-20 订正**：原写 `manual`，但 v1 不做手动框选（F5-8 只做「换回原图」「按 AI 矩形重裁」「换哪张图当封面」三件事），所以没有 `manual` 这个值 |
| **cover_index** | 数字 | 封面取自第几张原图 |
| my_note | 文本 | 我为什么存它 |
| **ai_raw** | JSON | **AI 最近一次成功识别的原值快照**（任务 6 新增，见下） |
| created_at / updated_at | 时间戳 | |
| status | 字符串 | `pending` / `done` / `failed` |
| share_token | 字符串 | 单条分享令牌，空表示未分享 |

**为什么单开 ai_raw 存一份 AI 原值（2026-09-20 · 任务 6）**：`ai_summary`／`category`／`source_platform` 这些字段是「用户当前看到的值」，用户一改，AI 原来给什么就永远查不到了。但有两处非要不可：

- **F5-6** 要求把 AI 原先的平台猜测**以黄色虚线框留在选项上供对照**——不存原值就画不出来；
- **F2-8** 要求记录「AI 原值 → 用户改动」的差异——没有基线就算不出差异。

所以 AI 说什么原样留一份，用户编辑概不触碰。内容是 `{category, categoryCorrected, categoryOutOfList, tags, summary, caption, ocrText, platform, rect, ms, at}`。

语义定为「**AI 最近一次成功的输出**」，重跑会覆盖：重跑是用户主动触发的一次重新识别，差异基线理应跟着移动，否则「我改了多少」会越算越离谱。

存量数据（本列上线前写入的素材）这一列是 NULL。界面据此**如实说明**「这条早于「记录 AI 原值」上线，矩形没留下来」，而不是顺口断言「AI 没给矩形」——后者是在断言一个查不到的事实。

**为什么加 ai_title（2026-09-20 · 任务 6 收尾）**：卡片上原本只有「摘要」，「这张图是什么案例」要读完整句摘要才知道。摘要 ≤50 字、AI 又写得偏描述性（「废弃高架货运铁路改造为城市空中公园」），扫一眼认不出是哪条素材。项目名是专有名词（「纽约高线公园」「诺邓古村盐井区更新」），辨识度远高于摘要。

- 由 AI 在**同一次调用**里一起给（提示词加 `title` 字段，≤12 字，判不出给空串），不额外增加一次请求；`normalizeTitle()` 去掉书名号/引号/空白并截到 20 字。
- 空串与 NULL 在界面上都显示「未命名」，但**语义不同**：空串是「AI 看了但判不出」，NULL 是「这条写在这个字段存在之前」。界面区分对待。
- 存量素材补名走的是**一次性工具** `app/_backfill_title.html`（补完已删除），它**只写 `ai_title`**：重跑 AI 会顺带给出不一样的抽屉和标签，那会把用户已经理好的分类改掉；也不写 `ai_raw`，避免造出一个「只有名字」的半截快照。代价是这几条补出来的名字**没有对应的 AI 原值可对照**——这是有意选的取舍。

**为什么缩略图单独放在 items 上，而不是和原图放一起**：列表页要一次取 20 条卡片。若缩略图与 1600px 原图同表，一次列表查询就会把几十 MB 图片拉下来。把 400px 缩略图做成 items 的一个字段后，列表查询是单表且只取这张小图（实测 20 张卡约 0.5MB），原图留给详情页按需取。

**标签为什么存「名字」而不是「ID」**：标签是开放集合，允许随时新增。存名字让「AI 生成一个新标签」不需要先建记录再引用，少一次写入；代价是改名要批量更新。考虑到标签总量在几百量级，这个代价可接受。

### 3.2 item_images（图片，新增）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | 自增整数 | 主键 |
| item_id | 外键 | 指向 items，级联删除 |
| seq | 数字 | 同一素材内第几张（0 起）；与 item_id 组成唯一约束 |
| mime | 字符串 | 统一 `image/jpeg` |
| width / height | 数字 | 像素尺寸 |
| byte_size | 数字 | 原始字节数（不含 base64 膨胀） |
| **data_base64** | 文本 | 图片本体，base64 编码 |
| **ai_rect** | JSON | **这一张图**被 AI 判出的主体矩形 `{top,right,bottom,left}`（百分比）。任务 8 新增 |
| created_at | 时间戳 | |

**为什么矩形要按图存一份**（2026-09-20，任务 8）：`items.ai_raw.rect` 是「**送进模型那张图**」的矩形。一旦用户在详情页把封面换成第 2 张图，`ai_raw.rect` 就对不上新图了——A14 的「重裁」当时因此失效（详情页判断条件是 `rect && cover_index===0`，换了图就永远不成立）。改成按 `(item_id, seq)` 存，重裁对任意一张封面图都成立（F5-8 第三条兜底）。

**图片统一规格（2026-09-20 决定）**：**长边统一 1600px、保持原始宽高比、不放大小图**，JPEG q88。长边不足 1600px 的图保持原尺寸——放大只增加体积、不增加信息。

### 3.3 categories（抽屉）

字段：`id`、`user_id`、`name`（唯一）、`is_fallback`（是否兜底，全库只能有一条）、`sort_order`、`created_at`。

初始数据（已写入，共 6 条）：城市与公共空间 / 建筑与构筑物 / 自然与生态 / 历史与遗产 / 表达与方法 / 其他 / 待归类（第 6 条 `is_fallback = true`，是兜底抽屉）。

> 订正（2026-09-20 实测核对）：本文档此前把兜底那条写成「其他」和「待归类」两项，实际库里只有一条，名为「**其他 / 待归类**」。共 6 条，不是 7 条。

### 3.4 tag_groups（标签用途分组）

字段：`id`、`user_id`、`name`（唯一）、`color`、`bg_color`、`sort_order`、`created_at`。

初始数据（已写入，色值与界面稿完全一致）：

| 组 | 主色 | 底色 |
|---|---|---|
| 参考与借鉴 | `#2E6FD9` | `#E8F0FD` |
| 应用场景 | `#B4652F` | `#FBEEE2` |
| 视觉与技法 | `#6B5AD6` | `#EFEDFC` |
| 对象类型 | `#3E7D57` | `#E7F2EB` |
| 问题与动作 | `#BC4265` | `#FBE9EF` |

### 3.5 tags（标签）与 settings（设置）

- **`tags`**：`id`、`user_id`、`name`（唯一）、`group_id`（外键）、`created_at`。已预置 **32 个起始标签**（取自 PRD 4.5 与界面稿演示数据），供 AI 优先复用。
  **颜色不存标签上，存分组上**（PRD Q-E 已确认「按用途分组同色」）。改配色只需改 5 条记录。
- **`settings`**：单条记录（`id` 恒为 1，带 CHECK 约束）、`share_enabled`、`share_token`（收藏夹分享令牌）、`updated_at`。
  **修订**：原方案里的 `owner_passcode_hash` **已移除**。原因见第 6 章——在路径 A 下口令只是前端门禁，把哈希存进一个匿名可读可写的库，既不提供安全，又制造「看起来有保护」的错觉。

### 3.6 体积实测（这套设计为什么可接受）

| 项目 | 实测值 |
|---|---|
| 缩略图 400px q72 | 平均 18KB → base64 24KB |
| 1600px 大图 q88 | 平均 155KB → base64 207KB |
| 真实小红书截图 1266×860 PNG 739KB | 转 1600px JPEG **仅 172KB**（截图转 JPEG 压缩率极高） |
| 20 张卡的列表查询 | 只取缩略图，实测响应体约 0.5MB |
| 300 条素材估算 | 约 45~60MB（若保留原始字节则 200~300MB） |

**未验证项**：平台数据库的总容量配额与单行硬上限。文档未暴露这些数字，标【待验证】。

### 3.7 建表状态（2026-09-20 已完成）

6 张表已实际创建，RLS 已开启，均 GRANT 给 `anon, authenticated`，并配置 `FOR ALL ... USING (true) WITH CHECK (true)` 策略；已写入抽屉 6 条、标签分组 5 条、起始标签 32 条、settings 单条。索引：`items(created_at desc)`、`items(category)`、`item_images(item_id)`、`tags(group_id)`。

**未登录读写已实测通过**：匿名调用者（`anon` 角色）可插入、读取、删除数据。

> 第 7 张表 `events` 于任务 8 追加，见 3.8。`item_images` 也在任务 8 追加了 `ai_rect` 列。

### 3.8 events（埋点，任务 8 新增，第 7 张表）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | 自增整数 | 主键 |
| user_id | 文本 | 恒为 `local-owner`（不做账号体系，没有第二个写入者） |
| name | 文本 | 事件名，见 PRD 第 9 章（`item_create_start` 等） |
| at | 时间戳 | 事件发生时刻，默认 `now()` |
| props | JSON | 事件自带字段（耗时、关键词、素材 id、设备…） |
| created_at | 时间戳 | |

索引 `events(at desc)`。RLS 同其余各表（`anon, authenticated` + `FOR ALL USING(true)`），因为未登录状态下访客也可能触发读；**写操作在 `store.js` 的 `insertEvent()` 里过 `guard()`**——也就是说这一层的门是前端门，和其余写入口一致。

**为什么不给 E1~E13 全部建事件**（这是任务 8 唯一一个真正的设计决定）：

判断标准只有一条——**这件事能不能从库里的当前状态算出来**。

| 事件 | 能不能派生 | 处理 |
|---|---|---|
| E4 改了哪些 AI 字段 | 能：`items.ai_raw` 对比现值，就是 F2-8 的差异 | **不写事件**，用时现算（`diff.js`） |
| E11 落进兜底抽屉 | 能：`category` 等于兜底抽屉名 | **不写事件**，现算 |
| E12 手动更正来源平台 | 能：`ai_raw.platform` 对比 `source_platform` | **不写事件**，现算 |
| E1/E2/E3/E5 耗时 | 不能：过程量，状态里留不下痕迹 | 写事件 |
| E6/E7/E8 搜索行为 | 不能：同上 | 写事件 |
| E14 一次检索如何收场 | 不能：同上（「没点开」这件事只发生在结算那一刻） | 写事件（2026-09-20 新增，见 9.8 第 7 条） |
| E9 会话 | 不能：同上 | 写事件 |
| E13 换封面 | 不能：换完只剩结果，看不出「是谁换的、为什么换」 | 写事件 |
| E10 访客访问分享 | 能，但**不做** | 见 9.8 第 3 条 |

结论：**14 条里实际写 10 条**。少写一处就少一处会漂移的副本——改两次的话，事件流还得靠顺序才能拼出「现在的差异」，而直接对比永远不会不一致。

---

## 4. AI 调用设计

**一次调用完成四件事**：输入原始内容（文本 + 图片），输出结构化 JSON：摘要、画面描述、图中文字、抽屉、标签。

### 4.1 关键设计点

| 点 | 做法 | 为什么 |
|---|---|---|
| **抽屉必须封闭** | 把当前抽屉清单作为**枚举**写进提示词，并要求只能从中选一个 | 这是 PRD F2-6 的硬约束，也是防止「素材越积越乱」的核心机制 |
| **标签必须参考已有标签库** | 把现有标签名一并给模型，要求「优先复用，确实不匹配才新建」 | 直接针对标签发散的失败模式 |
| **失败不能丢素材** | 先写入素材（状态 `pending`），再调 AI，成功后更新为 `done`。AI 挂了素材仍在 | PRD F2-7 |
| **结果必须可改** | AI 结果作为**默认值**回填，不是最终值 | 录入摩擦预算里已算进「确认 3 秒」 |
| **记录改动差异** | 用户改动时保存「AI 原值 / 用户改后值」 | 埋点 E4，唯一能量化 AI 标签可用率的办法 |

### 4.2 【已验证 · 2026-09-20】

**云服务的 LLM 模块支持图片输入（多模态）——已在真实调用中验证通过。**

验证方式：单文件验证页，用 `auto` 模型 + 一条真实小红书截图（1266×860，739KB → base64 986KB，以 data URL 直接传入）。

| 验证项 | 结果 |
|---|---|
| 模型目录是否声明支持 | 30 个模型中 **19 个显式声明 `supportsImages: true`**（含默认 `auto`）；10 个显式 `false`；1 个未声明（`hunyuan-image-v3.0`，不选它） |
| 真实调用是否成功 | 成功。`finish_reason=stop`，耗时 26.0s（其中思考链 675 tokens），首 chunk 4.6s |
| 图片是否真被看到 | 是。OCR 把截图里的标题、设计单位、室内面积、话题标签、发布日期全部正确提取 |
| 抽屉判断是否正确 | 是。判为「建筑与构筑物」，标签 `住宅 / 别墅 / 案例借鉴 / 海景住宅 / 极简建筑` |
| 来源平台预判 | 正确判为「小红书」 |
| 是否必须先把图片上传拿到公网 URL | **不必须**。base64 data URL 可直接传入 |

**结论**：走原路径，不启用降级路径。F2 的四件事（摘要 / 画面描述 / 图中文字 / 抽屉+标签）再由真实模型承担已无障碍。

**选型建议**：默认用 `auto`（平台自动匹配）。若关心成本，可选 `glm-5.3-flash`（x0.06）或 `deepseek-v4.1-flash`（x0.03，原生多模态）——本次调用 `auto` 消耗 0.76 credits。成本差异需在真实使用中观察，暂无依据断言哪个够用。

**【2026-09-20 新增发现】原方案第 3 章的图片存储设计不可行，已在评审中单独提出，待用户拍板后修订本章。**（详见 `构建记录.md` #012）

### 4.3 【实测 · 2026-09-20】AI 调用耗时封不住长尾

样本：5 张真实素材，模型 `glm-5.3-flash`，送模型的图压到长边 1000px。三轮独立采样，单位秒：

| 素材 | 轮次 1 | 轮次 2 | 轮次 3 |
|---|---|---|---|
| Ses Clotades（小红书截图 1266×860） | 5.8 | 16.6 | 15.5 |
| 纽约高线公园（1741×1011） | 11.4 | 6.7 | 8.2 |
| 2025 阿卡汗建筑奖（网页头图 700×449） | 10.5 | 11.2 | 41.7 |
| 衡水垂直农场（网页头图 700×449） | 3.7 | 3.2 | 3.9 |
| Ghaf Woods 体验中心（网页头图 700×449） | 3.5 | 4.4 | 3.7 |

**同一张图三次调用可以差 4 倍**（阿卡汗 10.5 → 41.7s）。所以 PRD F1-4 的「≤10s」不能当作 AI 识别步骤的硬指标（已按 R-5 改写）。

#### 时间花在哪里：首字延迟

对单次调用做了分段计时，结论明确——**耗时几乎全在「第一个字什么时候到」**：

| 指标 | 值 |
|---|---|
| 总耗时 | 57.8s |
| **首字延迟** | **51.2s** |
| 输出长度 | 996 字符 |
| 送模型的图 | 116KB / 1000×581 |
| 提示词 | 1160 字符 |

图片大小（全样本 62–178KB）与输出长度都不是瓶颈，**排队与预填充才是**。

由此**否掉一个看起来合理的方案**：把 OCR 拆成第二次独立调用。理由：既然每次调用的成本几乎都是固定的首字延迟，拆成两次就会把等待翻倍，换来的只是「短输出的那次快一点」。

#### 提示词长度是一个可控项，但不是主因

配对实验（同一张图、同一模型、前后脚）：

| 提示词 | 首字延迟 | 总耗时 | 输出 |
|---|---|---|---|
| 130 字（只问裁切矩形） | 5.9s | 6.4s | 84 字符 |
| 1160 字（原来的完整提示词） | 21.1s | 28.2s | 996 字符 |

提示词长度**确实**影响首字延迟，于是把提示词按「一句一条」重写、压到约 700 字。但重写后同一天又跑出 **43.3s 首字延迟**。所以准确的说法是：**服务端方差是主因，提示词长度是次要项**——重写值得做（它不损伤判断质量），但不能指望靠它解决长尾。

#### 并发：之前的结论过于武断

早先一次探测显示「3 张并发时其中一张 84.2s，而串行是 11.2s」，据此写了「并发更差，不采用」。但后来串行也测到 57.8s——**单次探测不足以支撑那个结论**。目前的处理是串行（不是为了躲限流，而是为了可预期），并且把「排队中 · 前面还有 N 条」显示出来，让人分得清「在排队」和「卡住了」。

#### 裁切判断的精度噪声

同一张高线公园（1600×929），AI 在不同时间给出的照片区宽度是 **68% / 70% / 59%**，人工标注是 69.1%。也就是说这个判断有 **±10 个百分点**的抖动，不是稳定值。处理方式：矩形明显不可信时（宽或高不足 25%）直接不裁用整图；能裁就裁，裁得不合适由详情页换回原图（F5-8，任务 6）。**这是已知限制，不是待修 bug。**

#### 实装后的实测（`app/index.html`）

| 环节 | 实测 |
|---|---|
| 粘贴 → 本地出图（含封面 + 400px 缩略图） | **21ms** |
| 入 items 表（卡片这一刻就可见） | **320ms** |
| 1600px 原件写 item_images | 约 700ms（放在卡片出现之后，不阻塞） |
| 识别成功样例 | 13.1s / 14.1s；失败样例 43.3s、46.5s 超时 |

即 PRD F1-4「本地 ≤1 秒出图入库」成立（≈340ms），且这个数字与 AI 无关。

#### 最终确定的录入流程形态（已落进任务 4）

1. **本地处理不等 AI**：先出图、先落库、卡片立刻可见；AI 字段留 `pending`。
2. **串行调用**，界面显示「排队中 · 前面还有 N 条」与「识别中 · 已 N 秒」。
3. **60s 硬超时 + 降级**：覆盖住观测到的上限（57.8s）。SDK 的 `createStreaming` 会把 `signal` 透传给 `fetch`（已在源码中确认），所以是 `AbortController` **真中断**，不是假超时。超时后封面退回整图、抽屉落兜底、标签留空，并给出「重跑 AI」。
4. **重跑失败不覆盖好结果**：如果这条之前已经识别成功过，重跑失败时只提示「这次没成功，下面是更早那次的结果」，不把状态改成失败。

**已知未解**：来源平台预判有噪声——同一条高线公园素材（网址 `thehighline.org`）第一次判成「公众号」（错），重跑后判成「网页」（对）。字段可改、后果轻，暂不为它加长提示词。

### 4.4 【实测 · 2026-09-22】网页正文抽取与三段设计说明总结

**为什么加这一步**：只填网址的条目，AI 此前只有「标题 + 描述」两串短文本可用。这对一句话摘要够（只要判「这是什么」），但要回答「**这个项目值不值得点进原文看**」就不够了——那需要知道它**怎么做**、**效果如何**，而这些信息只存在于原文正文里。用户的原话是「我的总结更像是对网页它的整体设计说明的一个总结，如果我感兴趣我才会点进去看」，这就是这一步的需求来源。

**正文怎么抽（`_page_meta.extract_body`）**：不引入第三方 HTML 解析库（「标准库之外零依赖」是本项目的底线），靠四步：

| 步骤 | 做法 | 为什么这么做 |
|---|---|---|
| 去噪声 | 按行丢掉导航 / 页脚 / 操作位的行（`_CHROME_LINE`，只匹配**动作位**特征，不做关键词粗过滤） | 关键词过滤会误杀正文——「分享」「返回」这类词在设计说明里是会正常出现的 |
| 找主体 | `_balanced_blocks()` 用**标签开闭配对计数**取 `article` / `main` / `div` 块 | 「取第一个 `<article>` 到它的 `</article>`」这种正则在嵌套块上会截错；配对计数是唯一稳的写法 |
| 双语去英 | `_drop_translation()`：全篇中文字符 > 200 时，只保留含中文的行 | gooood 是**中英对照**排版（同一篇正文各来一遍）。不去英则 token 直接翻倍，且英文半篇对中文总结零贡献 |
| 截断 | 上限 8000 字 | 防「一个五万字的页面」把请求撑爆 |

**送模型用多少**：`BODY_LIMIT = 6000` 字符。依据是实测 4 条真实文章的正文长度 **3,304–5,363 字**，而「效果」那一段通常写在**文章最后**——截太短恰好会把最需要总结的部分截掉。

**输出为什么是固定三行**：提示词里写死「固定三行，每行依次以「理念：」「做法：」「效果：」开头，每行一句，整段不超过 120 字」。固定三行是**为了可扫读**——写成一段散文，用户还是会略过。`normalizeDigest()` 兜住模型不听话的两种形态（给成数组、三行挤成一行），**但一段没带标签的文本不会被硬塞一个标签**——那是替模型编内容。

**耗时：这次上调超时，而不是缩正文**（同一条目重复三次）：

| 条目 | 第 1 次 | 第 2 次 | 第 3 次 |
|---|---|---|---|
| id=28（正文 5,203 字） | 33.1s | 34.0s | **67.9s** |

当时阈值 60 秒，第三次直接被掐断、落库为空。先怀疑是「提示词加长了」，于是做对照：长提示词与短提示词分开跑，**两条都在 34s 上下**。结论与 4.3 一致——**方差来自服务端排队，不来自提示词长度**，所以「缩正文换速度」是无效优化（代价还是丢掉「效果」段）。改成把 `config.aiTimeoutMs` 由 60s 提到 **90s**。

**顺带验证到的价值**：重跑 AI 后，两条历史摘要的**事实错误**被纠出来了——id=8 原摘要写「2025 阿卡汗奖」，正文里是「**第 16 届**」；id=11 原判「坡上…社区花园」，正文里是「**雅加达商业区下沉式商业空间**」。原因很清楚：原摘要只有标题 + 描述 + 首图，判断只能靠猜；读了正文才落得下来。

**已知限制（如实记录）**：`extract_body()` 只在 **gooood 一个站**上验证过（本次 4 条全是）。换站点的抽取效果未验证。抓不到时**不报错**，`page_text` 为空串，界面走「这条没抓到可读的网页正文」的空态。



---

## 5. 分享的实现（任务 7 已完成，2026-09-20）

| 分享类型 | 令牌位置 | 访客看到 |
|---|---|---|
| 收藏夹整体 | settings.share_token | 只读列表页，可按抽屉与标签浏览、可搜索、可翻页，**无任何编辑入口** |
| 单条素材 | items.share_token | 只有这一条，不含其他素材 |

**粒度是「整个收藏夹一个开关」，不做逐条勾选**（用户 2026-09-20 拍板）。上一版这里写的「只返回标记为可分享的素材」是逐条勾选方案的描述，**随该方案被否而作废**——收藏夹分享就是全量只读。

**关闭机制**：`share_enabled` 置否 **或** 令牌对不上 → 立即失效。两个条件都要满足才算有效，所以关闭时只置 `share_enabled=false`、令牌留着不删，链接也已经死了。

**链接形态**：`/s/令牌`（收藏夹）、`/i/令牌`（单条）。分享页同时兼容 `?t=令牌` 与 `#令牌`，因此**平台若不支持路径转发，只需改生成链接的那一处**。

**⚠ 路径式链接的硬前提：资源基准路径。** 浏览器打开 `/s/令牌` 时，页面的「目录」是 `/s/`，页面里所有相对路径（`style.css`、`config.js`、`cards.js`、`store.js`）都会被解析到 `/s/xxx` 而 404 —— **表现是页面卡在「正在打开…」的空壳，不报错**，极易误判成"后端没数据"。修法是在 `<head>` 最前面、任何资源之前动态写入 `<base>`，把基准指回应用目录（`/s/令牌`→`/`，`/app/s/令牌`→`/app/`）。这样根目录与子路径部署都成立。

**访客能看到的边界（必须守住）**：
- 收藏夹分享页：返回全部素材（该收藏夹就是这个人的私有库，没有"部分公开"这个概念）
- 单条分享页：只返回该令牌对应的那一条，其余一概不返回，**也不返回 `settings`**
- 两者都不返回 `ai_raw`、`my_note`（默认关闭）与任何设置表内容
- **访客拿不到 `?id=` 枚举的口子**：分享页点卡片是页内展开详情，不跳 `item.html`；`item.html` 靠 `?id=` 取任意一条，把它交给访客等于顺手给出「改个数字看全库」的办法

**实现要点**：
- `app/cards.js` 被「灵感库」与「分享页」共用 —— 卡的 HTML、侧栏、标签配色只有一份。分享页是对外的脸，靠复制一份 HTML 迟早会长得和主站不一样。
- `share.html` **刻意不加载 `ownerbar.js`**（访客看不到任何解锁入口），但**要加载 `auth.js`** —— `store.js` 的写操作都走 `auth.requireOwner()` 这道门，不加载反而会让分享页成为更弱的一面。
- 分享页带 `noindex,nofollow`：它是发给具体某个人看的，不该被搜索引擎收录。

---

## 6. 所有者保护（R-1，已于 2026-09-19 决定接受降级）

**这是本方案里唯一一个安全性降级点，必须讲清楚。**

做法：所有者在首次使用时设置一个口令；**口令哈希只存在该设备浏览器本地**（localStorage），写操作前在前端校验。分享视图隐藏所有编辑入口。

**2026-09-20 修订说明**：原方案写的是「前端保存口令的哈希」，但当时设想把它存在数据库的 `settings` 表里。改成路径 A 之后这条不成立了——路径 A 下数据库对所有匿名调用者开放读写，把一个「哈希」存进去既挡不住任何人，又会让人误以为有服务端保护。**因此 `owner_passcode_hash` 字段已删除，口令退化为纯粹的本地门禁。**

**它的真实强度（必须如实理解）**：

| 能防住 | 防不住 |
|---|---|
| 访客随手点开分享链接后乱改 | 懂技术的人用浏览器开发者工具绕过前端校验、直接调用数据接口 |
| 别人拿到网址后误操作 | 有明确意图的恶意篡改（可以改、也可以整库删） |
| 换一台设备访问自己的库（需要口令） | —— |

**为什么第一版可以接受这个降级**：
1. 库里存的是公开渠道收集的设计案例，不是敏感数据
2. 第一版只有一个用户，实际暴露面很小
3. 云服务对数据接口做了来源校验，第三方网站无法直接调用你的数据接口
4. 已实测确认：调用数据接口必须来自本应用的域名，且未登录者只能以 `anon` 角色行动——威胁面是「知道地址且愿意动手的人」，不是「随便一个访客」

**加固路径（保留，随时可升级）**：启用云服务的 Auth 模块，建立**单一所有者账号**，然后把数据库策略从 `USING (true)` 收紧为 `owner_id = auth.uid()`。数据表结构不必改（`user_id` / `owner_id` 列已预留），页面要加一个登录流程。注意这不违反「不做账号体系」——不做的是**多用户**，单所有者账号不构成多用户体系。代价是每个新设备多一次登录。

> **2026-09-20 用户已拍板**：走路径 A（不启用账号），接受上述降级。理由与取舍见 `构建记录.md` #012、#013。

---

## 7. 部署

| 事项 | 结论 |
|---|---|
| 部署方式 | 发布为在线应用（复用同一应用 ID） |
| 域名 | 平台分配，无需自备 |
| 手机访问 | 响应式布局覆盖，不需要单独做移动端 |
| 云服务激活 | **阶段四开工的第一步**，届时平台会弹出一个授权确认框，由你确认 |

---

## 8. 界面稿说明（`05-界面稿.html`）

界面稿是**可点击的原型**，用演示数据，没有任何后端。你可以：

- 点左侧抽屉、点标签、输入搜索关键词 → 看筛选怎么联动
- 点任意卡片 → 看素材详情（含改抽屉、改标签、分享入口）
- 点右上「收进灵感」→ 看录入流程与 AI 返回结果的呈现方式
- 点顶部「预览分享视图」→ 看访客看到的样子（编辑入口全部消失）

**演示数据里请重点看两处设计**：
1. **第 9 条是纯文本素材**（无图），卡片不是空白占位 —— 对应 PRD F3-2
2. **标签颜色跟着分组走**，不是每个标签一个颜色 —— 对应 PRD Q-E

---

## 9. 阶段四任务拆解

**进度（2026-09-20）**：任务 1、2、3、3.5、4、5、6、7、**8** 已完成。

| 序 | 任务 | 依赖 | 状态 |
|---|---|---|---|
| 1 | 激活云服务，拿到公开配置 | 需要你确认授权 | ✅ 2026-09-20 |
| 2 | 建数据表（6 张）+ 写入初始抽屉、标签分组与起始标签库 | 1 | ✅ 2026-09-20 |
| 3 | 验证 LLM 是否支持图片输入 | 1 | ✅ 通过，走原路径（见 4.2） |
| 3.5 | 图片处理管线（新增，路径 A 带出） | 1 | ✅ 2026-09-20，`app/pipeline.js` |
| 4 | 录入流程打通（含 AI 与失败兜底） | 2、3、3.5 | ✅ 2026-09-20，见 4.3 与 `app/add.html` |
| 5 | 素材库浏览 + 检索筛选 | 2、4 | ✅ 2026-09-20，见 9.1 与 `app/index.html` |
| 6 | 详情与编辑（含换封面） | 2 | ✅ 2026-09-20，见 9.2 与 `app/item.html` |
| 7 | 分享（两级）+ 口令保护 | 2 | ✅ 2026-09-20，见 5 与 9.6 / 9.7；`app/share.html` + `app/cards.js` |
| 8 | 埋点接入 + F2-8 改动差异 + 数据页 | 4、5 | ✅ 2026-09-20，见 9.8；`app/track.js` + `app/diff.js` + `app/stats.html`。**范围已按用户裁定收窄**：13 条事件实写 9 条，E4/E11/E12 改为派生，E10 不做 |
| 9 | 发布上线，开始记真实使用数据 | 全部 | ⬜ **注意：发布需单独获得同意** |
| 10 | 录满 30 条后复盘抽屉与标签（PRD A13） | 9 | ⬜ |

> 订正一处历史描述：任务 8 原写作「埋点接入（13 条事件）+ **F2-8 改动差异落库**」。实测后确认 **F2-8 不该落库**——差异是当前状态的函数（`ai_raw` 对比现值），落库等于存一份会漂移的副本。已改为派生，见 3.8 与 9.8。

**应用代码结构（`app/`）**：

| 文件 | 职责 |
|---|---|
| `config.js` | 唯一写死配置的地方：endpoint、publishableKey、user_id、模型、**超时（`aiTimeoutMs`，2026-09-22 由 60s 提到 90s，依据见 4.4）**、兜底抽屉、平台选项、口令哈希 |
| `auth.js` | 所有者门禁：`isOwner()` / `unlock()` / `lock()` / `requireOwner()`。纯前端本地门禁（R-1 降级，见 9.3） |
| `ownerbar.js` | 顶栏的所有者开关 + 就地解锁输入行。三页共用，并在 `<body>` 上打 `is-owner` / `is-visitor` |
| `pipeline.js` | 图片处理：归一 1600px / 裁封面 / 400px 缩略图 / 送模型的 1000px。不碰网络与数据库 |
| `store.js` | 数据访问：抽屉、标签库、素材、图片、列表索引、**埋点读写**、报表取数。**所有写操作都从这层过一道门禁**（F7-1）。`ITEM_COLS` 与 `INDEX_COLS` 的分工：`page_title`、`ai_digest` 两边都有（前者让「搜工作室名/项目原名」有效，后者让「搜正文里的专有词」有效），**`page_desc` 与 `page_text` 只在 `ITEM_COLS`**——正文体量大，进索引等于每次列表查询白拉几十 KB。**另**：`REPORT_COLS` 同步补 `ai_digest`（2026-09-22 收尾时查出漏了）——`diff.fields()` 加了 `digest` 行但它不在报表取数列里，`after` 会恒为空串，「只要 AI 出过这段总结就算被改过」，A2 字段表那行可用率恒为 0%。**与 #023 修的是同一类坑：不报错、但数字全错。** |
| `ai.js` | AI 识别：提示词、超时中断、抽屉白名单校验、字段归一、`snapshot()` 生成 AI 原值快照。**`detectRect()`**：只问矩形的短提示词通道，供详情页对任意一张封面图重裁（任务 8）。提示词吃 `pageTitle`/`pageDesc`/**`pageText`**（正文按 `BODY_LIMIT=6000` 截断，见 4.4）；并加硬约束「不要产出表示不确定/来源类型的标签」。产出含 `digest`，由 `normalizeDigest()` 归一（2026-09-22） |
| `cards.js` | **公开渲染模块**：卡的 HTML、侧栏、标签配色、搜索匹配、空态文案。**「素材库」与「分享页」共用同一份**，避免对外的分享页和主站长歪（见 9.6）。`_hay`（检索用的拼装文本）**含 `ai_digest`**，所以只出现在正文里的词（如 ETFE、夯土）也搜得到 |
| `diff.js` | **改动对照（F2-8）**：`fields()` / `changed()` / `summary()`。详情页与数据页共用同一套比较规则，两处各写一份迟早给出两个答案（任务 8）。字段表含 `digest`（2026-09-22 新增，v13 前识别过的条目 `ai_raw` 无此键 → 按「AI 没产出」不进分母）。`summary()` 每字段同时给 `produced`（AI 产出过非空内容的样本）/`empty`（没产出的）——A2 字段级分母只算 `produced` |
| `track.js` | **埋点**：`event()` / `session()` / `device()`。**检索结算**：`beginSearch()` / `touchSearch()` / `resolveSearch()` / `abandonSearch()`——一次检索必须落到 `found` / `timeout` / `abandoned` 之一，A4 的分母取自它（见 9.8 第 7 条）。约定：**埋点失败只 `console.warn`，绝不 reject**——记录行为不能反过来把主流程搞坏（任务 8） |
| `index.html` | **素材库（首页）**：侧栏抽屉/标签 + 卡片栅格 + 搜索 + 筛选 + 看原始截图 + 收藏夹分享面板（仅所有者可见） |
| `add.html` | **录入页**：粘贴/拖拽/点选 → 本地入库 → AI 异步补齐 → 就地改 → 看原始截图。**只填网址时先抓原文**（`/api/meta`、`/api/img`，抓不到照常入库）。A1 计时起点是「本条第一个输入动作」（`itemStartedAt`），不是页面打开时刻 |
| `item.html` | **素材详情**：**该条实际拥有的原图**（可放大）/ 原文 / 来源 / 摘要 / **设计说明总结（可改）** / 抽屉 / 标签 / 备注 / 换封面 / **改动对照** / 删除 + 单条分享面板（仅所有者可见）。**两处状态区分（2026-09-22）**：① 访客态下编辑区统一为只读视图——操作引导文案对访客无意义，一律不渲染，chip 也不再显示可点手势（`body:not(.is-owner) .chip{cursor:default}`）；② 「设计说明总结」空态分三种（有总结 / 有正文但 AI 判非设计项目 / 没抓到正文），**不合并成一句「暂无」**，否则用户无法判断要不要重跑；③ **窄屏保存条**（`≤980px`）由 `position:sticky` 改 `position:fixed` 贴底——`sticky` 的活动范围受包含块（`.col-fields`）限制，单列布局下包含块起点在首屏之下，等于不生效；改 `fixed` 后给 `main.detail` 补 `padding-bottom:112px` 防末张卡片被遮。**这三条都写在 item.html 自己的 `<style>` 里**（页面私有样式不进 `style.css`） |
| `share.html` | **访客只读页**：认 `/s/令牌`、`/i/令牌`、`?t=令牌`、`#令牌` 四种入口。**不加载 `ownerbar.js`**（无解锁入口），但加载 `auth.js`（写操作仍要过门禁）。点卡片页内展开详情，不跳 `item.html` |
| `stats.html` | **数据页（仅所有者）**：验收进度 + 每日录入条数 + AI 字段修改率 + 找回效率 + 兜底抽屉占比 + 封面人工兜底。**门槛只有一个来源（`stats.js`），不到门槛不给任何百分比**，只报「还差多少」；A13 在 30 条之后只显示中性的「待人工复盘」，不代判达标（任务 8） |
| `style.css` | 三页共用样式 + **唯一的权限样式**（`body:not(.is-owner) .owner-only{display:none}`）。页面私有样式仍写在各自 HTML 的 `<style>` 里 |
| `_serve.py` | 静态服务，本地与线上同一份：`/s/*`、`/i/*` 重写到 `share.html`（保留地址栏）、强制 `Cache-Control: no-store`（裸 `http.server` 会缓存旧 JS，导致误判）。**挂 `/api/meta`、`/api/img` 两个端点**（见 `_page_meta.py`）。**线上就是靠它跑起来的**（平台以单端口 HTTP 服务方式运行，所以路径式分享链接成立）：带 `PORT` 时监听 `0.0.0.0`，用 `ThreadingTCPServer`（前面有反向代理，单线程会让健康检查与并发请求互相排队） |
| `_page_meta.py` | **抓目标网页的标题 / 描述 / 首图 / 正文**（只填网址的采集，纯标准库无第三方依赖）。为什么必须服务端：gooood 不发 CORS 头，浏览器既拿不到 HTML 也拿不到图片字节，而「取字节→canvas 缩 400px」必须同源。**正文抽取**（2026-09-22 新增，见 4.4）：去导航/操作位噪声 → 标签配对计数取 `article`/`main`/`div` 主体 → 双语站按「全篇中文 > 200 字」去掉英文半篇 → 上限 8000 字。**SSRF 防护**：只放行 http/https、DNS 解析后逐地址校验只认公网地址、逐跳重校验（上限 3 跳）、超时与总预算、响应体字节截断、校验 Content-Type。`/api/meta` 的返回体因此多了 `text` 字段（抽不到即空串） |
| `_verify_*.html` | 一次性验证台（管线 / 多模态 / 匿名读写 / 裁切），**结论落地前不要删** |

**任务 4 之前必须先做的一件事**：图片处理管线（统一转 1600px / 保比 / 不放大 → 生成 400px 缩略图 → 裁出封面）。这是路径 A 带出来的新增工作，原任务表里没有。

### 9.1 浏览页的两个查询设计（任务 5）

素材体积实测（见 3.6）决定了列表页不能整表拉图片。浏览页因此拆成两步取数：

| 方法 | 取什么 | 为什么 |
|---|---|---|
| `loadIndex()` | 只 SELECT 文本字段、**不含 `cover_thumb`**，按 `created_at desc` | 首屏快；几百条素材也不会一次拖下几十 MB |
| `cardsByIds(ids)` | `.in('id', ids)` 批量取 `id / cover_thumb / cover_source / status / category` | 只给当前页要显示的那些卡取缩略图 |

`ensureCards(page)` 会在批次返回后递归补拉一次：请求在飞的时候新写入的素材，否则会从列表里漏掉。

**侧栏必须包含 AI 新造的标签。** 侧栏标签基于 `countByTag()`（按素材实际打标统计）渲染，而不是遍历 `tags` 表。`tags` 表里没有、但素材上已经有的名字，归到一个叫 **「未分组（AI 新造）」** 的分组里，同样可点可筛。否则这些标签在筛选栏里不存在，F4-3 会静默漏掉一部分内容。

**筛选组合关系**：搜索（F4-1）、抽屉（F4-2）、标签（F4-3，多选）三者之间一律 **AND**；标签多选也是 AND（取交集，不是并集）。叠加关系见 F4-4。

### 9.2 详情页（任务 6）

形态选**独立页面** `item.html?id=N`，不是弹层。理由：单条分享（F6-2）本来就要一个可复制的独立地址，弹层做完到任务 7 还得重做一遍。

**字段的保存时机分两类**，这个区分是照 F5-2 的字面要求来的：

| 类型 | 字段 | 行为 |
|---|---|---|
| 点了就存 | 抽屉、标签增删、来源平台 | 立即写库并刷新界面 |
| 攒着一次存 | 摘要、画面描述、图中文字、备注、来源链接、原文 | 点「保存」按钮 |

一次点击存一次会造成快速连点时请求乱序落库，所以详情页的写操作在客户端排成一条串行链。

**F5-8 换封面只做三件事**（2026-09-20 与用户确认的范围，不含手动框选）：

- **用整图（不裁）** → `cover_source='raw'`，由 1600px 原件整图重做 400px 缩略图
- **按 AI 矩形重裁** → `cover_source='crop'`，矩形取 `ai_raw.rect`；矩形被 `normalizeRect` 判为不可信时会退回整图，此时界面**明确告知是「退了」而不是装作裁了**
- **换哪张图当封面** → 改 `cover_index` 并重做缩略图。多图时才显示这个入口（单图没有意义）

「重裁」只在 `cover_index === 0` 时出现：AI 的矩形描述的是当初送给模型的那张图的版面，换了图之后套用旧矩形式错的。

**一个在真实浏览器里才暴露的 bug（已修，值得记下来）**：左栏原本是 `position:sticky;top:74px`。竖版封面（裁切后 709×860）在 440px 宽的栏里高约 530px，把换封面的两颗按钮推到 679px 处；而 sticky 会把整栏钉住，于是**不管怎么往下滚，按钮都停在 577px 高的视口之外——完全点不到**。
修法是两头收：封面预览限高 `44vh`；跟随滚动只在 `min-height:900px` 时开，并且加上 `max-height:calc(100vh - 92px); overflow-y:auto`。**可达性由结构保证，不再靠估算窗口高度**——只加「窗口够高才钉」的话，多几张图或描述变长又会失效。页面加载后零 JS 报错。

### 9.3 口令门禁（任务 6 提前落地，属 F7）

F5-4（访客隐藏编辑与删除入口）是 P0，没有「谁是所有者」这个概念就实现不了，所以 F7 的口令机制被提前到任务 6 做掉最小可用版本。任务 7 只需在其上加分享链接、只读视图与 F7-4 的输错频率限制。

- 口令的 **SHA-256 存在前端** `config.js`，比对在浏览器里做，解锁状态记 localStorage。R-1 已接受这个降级：它拦的是**误操作与路人**，不是攻击者。真正不可逆的动作（任务 9 发布）单独拿同意，不依赖它。
- **依赖 secure context**：`crypto.subtle` 只在 https 或 localhost 可用。本地走 `127.0.0.1` 没问题；若用局域网 IP 打开，`unlock()` 会明确报错说明原因，而不是静静地永远解锁不了。
- **权限判断只有一处**：`<body>` 上的 `is-owner` / `is-visitor`，配合 CSS `body:not(.is-owner) .owner-only{display:none}`。页面不各自判断「我是不是所有者」——判断散在各页就一定会有漏的。详情页更进一步：访客态下编辑控件**根本不渲染**，不是渲染完再藏。
- **写操作在 `store.js` 一处设卡**（`guard()`）：将来新增写入口会自动被挡住，不必记得每处加判断。

### 9.4 卡片的信息顺序（任务 6 收尾，按用户扫读习惯定）

用户给的顺序是：**抽屉 + 收藏日期同一行 → 项目名 → 摘要 + 标签 → 操作条**。日期从底部操作条挪到抽屉旁边，空出来的位置放项目名。

`228px` 宽的卡片里，项目名和「看原始截图 / 原文」两颗按钮同一行只够显示 5 个字，等于白放。所以操作条整体挪到卡片最底部（`margin-top:auto` + 上边框），项目名独占一行（`13.5px` 加粗、单行省略号）。**这不是审美调整，是宽度算术的结果**——先算清一行能放多少字，再决定谁跟谁同行。

搜索的 haystack 同步纳入 `ai_title`，placeholder 也改成「搜项目名 / 摘要 / …」，否则「有这个字段但搜不到」比没有更费解。

### 9.5 顶栏在窄屏会挤坏（实测发现，已修）

顶栏是不换行的 flex，子项（logo + 导航 + 右侧说明 + 所有者开关）在 `<420px` 时被 flex-shrink 压到内容宽度以下，**「灵感库」被拆成逐字竖排**，同时整页多出横向滚动条。560px / 768px 正常，所以只在手机宽度暴露。

两处修法：

- `.hnav` 加 `flex:none`、`.hnav a` 加 `white-space:nowrap`——宁可挤出去也不能把中文拆成竖排。
- 共享样式里补两级降级（`style.css`，三页同享）：`≤760px` 丢顶栏副标题（`header > .muted`，按位置选而不是给某个 id 打补丁，因为三页副标题写法不一样），`≤460px` 再丢 logo 文字（`font-size:0`，只留图标）并缩导航与按钮字号。

实测 390 / 430 / 560 / 768 / 1280 五档：顶栏高度均为 58px，导航单行，无横向溢出。

**这里踩的坑值得记**：改完 CSS 用 `set viewport` 复测时发现「没生效」，其实是**页面根本没重新加载**——`set viewport` 只改视口，不触发导航。必须重新 `open` 一次才拿到新 CSS。差点据此判断修改无效而回退。

### 9.6 分享页的实现（任务 7）+ 两个实测踩到的坑

代码结构：新增 `app/cards.js`（公开渲染模块）与 `app/share.html`（访客页）。

`cards.js` 的接口：`esc / fmtTime / toDataUrl / tagChip / counts / navCatsHtml / navTagsHtml / cardHtml / haystack / matches / emptyHtml / countText`。其中 `cardHtml(it, thumbRow, tax, opts)` 的 `opts={readOnly, actions, openAct}` ——**访客态靠 `readOnly:true` 收起主站才有的「看原始截图 / 原文」按钮**，否则访客会以为这里是能用的一整套应用。

`share.html` 的四种入口归一：`/s/令牌` → 收藏夹；`/i/令牌` → 单条；`?t=令牌` 与 `#令牌` → 不知道哪种，先当收藏夹校验、再当单条查（两个命名空间都是随机令牌，撞车概率可忽略）。

**坑一：空壳不报错 —— 路径式链接下资源全 404**

路径式链接打开后页面卡在「正在打开…」。原因是浏览器把页面「目录」认成了 `/s/`，页面里所有相对路径被解析到 `/s/style.css`、`/s/config.js` …… **全部 404，本地脚本一个都没跑起来**（只有 CDN 那个绝对地址的 SDK 加载成功，所以更迷惑——看起来"网络是通的"）。**症状是空壳而不是报错**，不逐项点开就会误判成"数据库没数据"。

修法：`<head>` 最前面、任何资源之前动态写入 `<base>`：

```js
var p = location.pathname, i = p.search(/\/(s|i)\//);
var dir = i >= 0 ? p.slice(0, i + 1) : p.replace(/[^/]*$/, '');
document.head.appendChild(Object.assign(document.createElement('base'), { href: dir }));
```

`/s/令牌` → `/`；`/app/s/令牌` → `/app/`；`/app/share.html?t=令牌` → `/app/`。根目录与子路径部署同时成立。

**本地服务 `_serve.py`**：把 `/s/*`、`/i/*` 重写到 `share.html`（**保留浏览器地址栏**，分享页才能从 pathname 读到令牌）。同时支持 `PORT` 环境变量与 `0.0.0.0` 绑定，作为任务 9 发布入口的雏形。

**坑二：CSS 类名撞车（同一个 `empty` 身兼两职）**

分享页截图暴露出「画面描述」「图中文字」两个空字段各占两百多像素高、内容还居中。根因是**一个类名被用在了两种语义上**：

| 用法 | 元素 | 期望样式 |
|---|---|---|
| 整页空态（"没找到，试试换个词"） | `.empty` | `text-align:center; padding:80px 20px` |
| 空字段小字 | `.ro.empty` / `.csum.empty` / `.sum.empty` | 只要灰一点的小字 |

后三者都带 `empty` 类，于是把前者的 80px 内边距与居中一起继承了。**这个 bug 在任务 6 的详情页就已存在**，只是当时没逐字段量高度；分享页把它放大成"访客看到一整屏空白面板"，才被看见。`csum.empty` 那条更隐蔽 —— 它会让「没有摘要」的卡片整体被撑高，而当前 3 条素材都有摘要，属于潜伏状态。

修法：整页空态改名 `.emptystate`（`style.css` 1 条规则 + `index.html`、`share.html` 共 3 处 `class`），其余保持原样。实测 `.ro.empty` 高度 228px → 21px。

**教训**：给"状态"起类名时不要用 `empty` 这种通用词。同一套样式表里，通用词一定会被别处复用，而复用的时候没人会去查它原本绑了什么样式。

### 9.7 口令输错频率限制（F7-4，任务 7）

用户定的档位：**连错 5 次锁 60 秒**。

- 计数 `elangit.owner.fails`、锁定时刻 `elangit.owner.lockUntil` 都写 localStorage —— **刷新页面不能绕过**。若只放内存变量，按一下 F5 限制就没了，等于没做。
- 锁定期内 `unlock()` 连哈希都不算，直接返回 `false`：让「锁定」在代码里是显式一步，而不是靠界面自觉不去调用。
- 触到上限时把计数清零、开始计时：锁是这一轮的结束，解锁后重新从 0 数。
- 界面显示实时倒计时（`setInterval` 每秒走）。定时器必须是**模块级变量**：`paint()` 会换掉顶栏整块 HTML，局部变量管不住它，会漏出一个永远在跑的定时器。
- **不做递增惩罚**：用户只指定了这一档，多出来的档位属于没被要求的行为。

**如实记录它的强度上限**：这是**纯前端限制**。清浏览器数据、或直接在控制台调 `auth.unlock()`，都能重置。拦的是手滑，不是破解。

### 9.8 埋点、改动对照与数据页（任务 8）

#### 1）F2-8「改动差异」为什么是派生，不是事件

任务表里原来写的是「F2-8 改动差异落库」，实装时推翻了这个说法。**差异是当前状态的函数**：`items.ai_raw` 是 AI 当时说了什么，`items` 上是现在是什么，一比就是差异。存成事件流反而多一份会漂移的副本——改两次就得靠事件顺序才能拼出「现在的差异」，而直接对比永远不会不一致。

于是比较规则单独成一个模块 `diff.js`，**详情页与数据页共用**。不共用的代价是具体的：详情页说「改了 3 处」、数据页说「改了 2 处」，同一份数据两个答案，比没有这个功能更糟。

比对哪六个字段：项目名称 / 摘要 / 画面描述 / 图中文字 / 抽屉 / 来源平台（比对对象是 `ai_tags`，不含 `my_tags`——用户自己加的标签不是对 AI 的否定，没有「AI 原值」可对照）。`my_note` / `raw_text` 同理不在内。

**没有 `ai_raw` 的素材要如实说「没留下来」，不能写成「没改过」**——后者是在断言一个不知道的事实。`ai_raw` 是 v7 才加的列，库内 3 条存量素材全是 NULL，数据页据此显示「还没有样本」而不是「0% 修改率」。

#### 2）A14 的真修（原代码在换过封面后永远修不好）

**症状**：截图类素材换了封面图（`cover_index = 1`）之后，详情页的「按 AI 矩形重裁」点不动。

**根因**：矩形的唯一来源是 `items.ai_raw.rect`，那是「**送进模型那张图**」的矩形；而重裁的守卫条件写的是 `rect && cover_index === 0`。用户在详情页把封面换成第 2 张图，`cover_index` 变成 1，条件永远不成立。换句话说，**换过封面就再也修不回来了**，而「换封面」正是最容易需要重裁的场景。

**修法（两层）**：

1. `item_images` 加 `ai_rect` 列，矩形**按图存**（见 3.2）。重裁的对象从「第 1 张」改成「**当前封面那张**」（`state.item.cover_index`），守卫条件里的 `cover_index === 0` 删掉。
2. 目标图还没有矩形时，用 `ai.js` 新增的 `detectRect()` 现场问一次（短提示词，只要矩形，比完整识别快得多），问完写回 `item_images.ai_rect` 并缓存。界面上这就是「让 AI 判这张图的画面区（要等一会儿）」这个按钮。

**实测**：对 #2（纽约高线公园，封面是第 2 张图）点一次 → 约 10 秒 → 封面按 AI 矩形重裁，比例 400×369；库里 `item_images seq=1 ai_rect={"top":0,"right":63,"bottom":100,"left":0}`、`cover_source='crop'`；`cover_replaced` 事件带上 `reason='recrop'` 与 `rect_fresh=true`。**这条验收项现在是可修的了，任务 6 时它是修不了的。**

#### 3）E10（访客访问分享）为什么不做

和 F6-6 同一个理由：事件表是 `anon` 可写的（不做账号体系就没有别的选择），**匿名写入意味着客户端可以伪造任意一条**。在一个能自己造数的表上统计「分享被看过几次」，得到的数字不可信，而不可信的数字比没有数字更坏——它会以「数据」的样子被引用。

代价要说明白：**这一版拿不到分享的实际价值量化**。真要，得先有账号体系（v2），或者在分享页上让访客写一条带服务端校验的记录。

#### 4）埋点失败必须旁路

`track.js` 的硬约定：**任何埋点写入失败只 `console.warn`，不 reject、不 throw**。调用方是录入、搜索这些主流程，记录行为不能反过来把它们搞坏——埋点是观察者，不是参与者。`store.insertEvent()` 因此对访客返回 `false` 而不是抛错。

#### 5）实测踩到的两个坑（都会静默丢数据）

**坑一：E7 在跳转前发请求，必然被取消。** `search_result_click` 的语义是「从搜索到点开的耗时」，所以它得在点卡片、`location.href` 跳转**之前**发。但浏览器在页面卸载时会取消未完成的 `fetch`，于是这条事件**系统性地一条都到不了库**——而它恰恰是 A4 唯一的判据。修法：`goAfterLog(href, logPromise)`，先把写入发出去并给 400ms 预算，然后才跳转。实测 `ms=2264` 正常落库。
（这不是埋点的坑，是「用跳转前的最后一个请求做记录」这件事本身的坑。）

**坑二：`session()` 的顺序写反了。** 原实现先写 localStorage 的「今天已记过」标记、再调 `event()`，而 `event()` 对访客返回 `false`。结果：**访客访问一次，标记照样落上**；所有者随后在同一天打开，`session_open` 就被这个标记挡掉了——日志里一条都没有，看起来像功能没做。修法：先 `isOwner()` 判断，**写入成功之后**才落标记。

#### 6）数据页的取数口径

| 面板 | 数据来源 | 样本不足时 |
|---|---|---|
| 验收进度（A1/A2/A4/A12/A13） | 事件 + 素材 | 直书「样本不足（n / 目标）」 |
| 每日录入条数（A12） | `items.created_at` | 看的是**有没有掉零**，不是总量 |
| AI 字段修改率（A2） | `diff.summary()` | 「还没有样本」，不含 NULL 的素材进分母 |
| 找回效率（A4） | **E14 结算事件**（E6/E7 照记，但不再用来算比例） | 「还没有结算记录」；若只有改版前的 E6/E7，则说明它们「配不成对」、无法判断哪些检索没找到 |
| 兜底抽屉占比（A11/A13） | `items.category` | 不到 30 条不给占比，只给条数分布 |
| 封面人工兜底（E13） | E13 | 「还没有人工换过封面」——这是好事 |

取数只走 `reportItems()`（**不带图片**，`ai_raw` 与 `ai_summary` / `ai_caption` / `ocr_text` 必须带——前三者是 A2 字段表的对照物，少一列那三行的可用率就恒为 0%，见第 7 条）。日期分组用**本地时间**，不用 UTC——否则晚上 8 点后录的素材会被算到第二天，「连续两周」直接判错。

#### 7）A4 的口径修正与数据页的三处订正（2026-09-20，外部复审后）

外部复审（Codex）提出四条，逐行回读代码后**全部成立**，已修：

| # | 问题 | 根因 | 修法 |
|---|---|---|---|
| 1 | A1 取到的不是「最早 20 条」 | `EV_SINCE_DAYS = 60` 的窗口 + `loadEvents()` **倒序** + 在倒序结果上 `slice(0, 20)` → 实际是「最近 60 天里最新 20 条」 | 新增 `store.loadFirstEvents(name, limit)`：按 `at` 正序 + limit，脱离窗口。**样本窗口是查询的属性，不能在渲染层补救**——顺序错了均值就是另一个数，而且看不出错 |
| 2 | A2 对全量算，没截「前 50 条」 | `diff.summary()` 收到的就是全量素材 | `summary(items, opts)` 加可选 `firstN`：先取最早 N 个**有原值**的样本再聚合。`withoutRaw` 仍按全量算，否则那句「还有 N 条旧素材没有原值」会变成错的 |
| 3 | A4 的分母被系统性做小 | 只对 E7（点开过的）算比例——**发起检索但根本没点开的次数一条都留不下**，找回成功率被高估 | 新事件 **E14 `search_attempt_end`**：开一笔检索就欠一笔结算 |
| 4 | A13 满 30 条必显「未达标」 | 判定写死 `verdict(false, n, 30)` | 改三态：不到 30 条「样本不足」；够了显示**「待人工复盘」**——数据页读不到 `构建记录.md`，判不了「复盘做过没有」，不假装知道 |

**顺带发现并修掉的第三个 bug（复审没提）**：`reportItems()` 的 `REPORT_COLS` 少了 `ai_summary / ai_caption / ocr_text`，而 `diff.fields()` 正是拿这三列跟 `ai_raw` 比。于是「只要 AI 出过这段文字就算被用户改过」，A2 字段表那三行的可用率**恒为 0%**。不报错、但数字全错，是最难发现的那一类。这三列只有数据页要用（列表页走 `ITEM_COLS`），加进去不影响别处。

**E14 的结算机制**（`track.js`）：
- `beginSearch(cond)` → 返回 `search_id`，形如 `<页面会话id>-<序号>`。**不能用 `sid()`**：那是设备级持久值，刷新后不变，会把不同次、不同天的检索混成一个 id。开新的一笔时**先把上一笔结算为 `abandoned`**——用户又搜了，说明上一次没找到目标。
- `resolveSearch(extra)` → 按耗时判 `found`（≤ 10 秒）/ `timeout`，结算后清空当前笔。所以**同一次检索连点两张卡片，分母只加 1**。
- `abandonSearch()` → 清空筛选、离开页面时结算。清空筛选**仍然不记 E6**（原来的判断成立：那不是找东西，是放弃找）。
- E6 / E7 的语义不变，仍照记，只是都带上同一个 `search_id` 便于回溯；**A4 不再读它们算比例**。

**已知限制（如实记录，不是待修 bug）**：`abandoned` 只能在页面卸载那一刻触发，而浏览器会取消卸载时未完成的 `fetch`——这个坑项目已经踩过一次（E7 曾系统性全丢）。所以 `abandoned` 会偏少、A4 的分母会略小。想估这个偏差有多大，用「同一窗口内 E6 的条数」减去「E14 里非 abandoned 的条数」；数据页把这组旁证一并显示了。

**门槛只有一个来源**：`stats.js` 的 `GATES = { A1: 20, A2: 50, A4: 10, A13: 30, A12: 14 }`，`gateText()` 统一产出「样本不足（n / 目标）」。四个渲染函数不再各自写 `< 10` / `< 30` 这类散落的比较——改口径只改一处，而且改它会逼着同步改 PRD 的验收表。

**这一步是怎么验的**（不能只凭「页面看起来正常」）：
- `app/_verify_stats.html`（一次性验证台，**61 条断言**）：造样本跑临界值 19/20、49/50、9/10、29/30；造「修好前 vs 修好后」两种数据形状做对照；`track.js` 的结算逻辑也在里面（连点两张只加 1、又搜一次结算为 `abandoned`、15 秒后才点开判 `timeout`——用拨时钟代替真等 10 秒）。跑法：本地服务 + 浏览器打开；页面里另有一个「连真实库探针」按钮，验 `reportItems()` 到底取回了哪些列（本次实测：八列齐全）。
- 第一条断言就是这条修正的意义：**同一批事件，新算法 10.5 秒、旧算法 60 秒**——差 6 倍，且两边样本一条都不重合。
- 真实库回归：素材库 / 录入 / 详情 / 分享（`/s/令牌` 与 `?t=令牌` 两种形态）/ 数据页五个页面**无 JS 报错**；3 条素材的真实库里所有面板都正确显示「样本不足」，没有百分比。
- **门槛之上的分支在真实库上走不到**（3 条素材 vs 门槛 20/50/10/30）。用 `tools/make-verify-ui.py` 生成 `app/_verify_stats_ui.html`：**渲染代码与 `stats.html` 逐字节一致**（已核对），只插一段合成数据桩（60 条素材 / 20 次录入 / 12 次检索）。实测 A1 14.5s、A2 88%、A4 83%、A12 14 天、A13「待人工复盘」、兜底 12% 全部正确显示，页面无报错。

#### 8）发布上线（任务 9，2026-09-20）

线上地址 **https://elangit.app.workbuddy.host/**。平台跑的是我们自己的 Python 服务（`deployedAs: http-service`），**不是静态托管**——这一点决定了分享链接的形态能不能保住。

**★ 发布前拦下的坑：云服务对请求来源做 Origin 精确匹配**

发布前做了一次「同一份发布件、同一个进程、只换来源」的对照：

| 浏览器来源 | 结果 |
|---|---|
| `http://127.0.0.1:8796` | 正常，3 条素材全部渲染 |
| `http://192.168.1.104:8796`（本机局域网） | 整页停在「加载中…」，约 20 秒后报 `读素材索引失败：TypeError: Failed to fetch`；**控制台一条错都不报** |

`Failed to fetch` 是浏览器在网络层/CORS 层拦下请求的典型错误。两份文件只差 `config.js` 里的 origin，所以结论明确：**只有应用的保留域名被放行**。若没提前试出来，发布会的表现就是「发布成功、链接能打开、但读不出任何数据」——而控制台干净，极难归因。

→ **修法不是改代码，是复用应用 ID**：`wbapp_az0Z1pxT1CjCvbUNffduqc` 复用才能保住保留域名与云服务校验的匹配关系。**绝不能新建应用**（新应用拿到的是不匹配的域名）。

**新增一个构建步骤：`tools/build-deploy.py`**

不能直接发布 `app/`：目录里有一批一次性验证件（`_verify_stats.html`、`_verify_stats_ui.html`、`_verify_shots/`），平台会把整个目录压缩上传，不做筛选。该脚本挑出 17 个产品文件组装到 `~/Documents/Elangit-部署/`，并自查三条：①与 `app/` **逐字节一致**（只挑不改）；②验证件 0 混入；③`config.js` **确实保留了真 key**（这一条是要求——脱敏了应用就连不上库）。

**两条流水线方向相反，必须分清**：

| 脚本 | 去处 | 凭据 | 原因 |
|---|---|---|---|
| `tools/push-to-github.py` | 公开**仓库** | 脱敏 | 仓库是源码快照，不需要能跑 |
| `tools/build-deploy.py` | 公开**运行的应用** | 保留真 key | 纯前端直连库 + 分享链接要让外人读得到，架构上不存在「只有所有者持有 key」 |

**`_serve.py` 改为 `ThreadingTCPServer`**：单线程本地看不出问题，但线上前面有反向代理，代理健康检查与浏览器并发请求会互相排队，表现成「偶发卡住几秒」。实测并发 6 个请求 0.37 秒内并行返回。

**怎么验的**

- 本地以发布方式起服务（`PORT=8797 python3 _serve.py`）：监听 `*:8797`（0.0.0.0）；`/`、`/s/令牌` 200，`/_verify_stats.html` **404**（排除生效），`Cache-Control: no-store` 保持。
- 线上：`/` 200（19487 B）、`/stats.js` 200、`/config.js` 200、`/share.html` 200、`/s/2zs9q7nzpcz4avg8` **200 / 18186 B = share.html**（**服务端重写在平台上成立**），`/_verify_stats.html` 404。
- 线上浏览器实测：首页读出 **3 条素材**（读库正常的标志）；**手机视口 390×844 无横向溢出**；录入页 / 数据页正常（未解锁时显示门禁文案）；整库分享页显示「共 3 条素材 · 只读」并渲染 3 张卡片；全程 **JS 报错 0**。

**如实记下的风险**：key 现在随页面下发到公网（架构决定，见第 10 节 R-1 与 AGENTS.md 第 10 节）；缓解项只有可被伪造的 Origin 校验；**撤销发布不收回 key**；因此云库不能当唯一副本，需定期导出兜底。

#### 9）首次真实录入测试的 8 个问题修正（2026-09-21）

用户把自己攒的 **16 条 gooood 链接**（`iCloud/卢宇/灵感库测试/测试.txt`）交给我做一次真实录入 + 测试。在**线上环境**、按真实操作（只填网址、来源平台保持「让 AI 判断」、16 条一次性连录）跑完，找出 8 个问题，用户拍板后修掉。四个决策：① 16 条随机保留 3 条（id 8/11/15）用于后续测试，其余 13 条 + 4 条修 bug 时的测试残留一并删；② P0 两条口径错现在修；③ P1-1「存网址没封面」选 **B**（服务端抓标题首图，架构级改动）；④ 问题清单整理成公开版进作品集。

**P0-1　A1 计时起点错，连录多条时系统性虚高**（已修）

`add.html` 原用 `pageOpenedAt = Date.now()` 当起点，E5 记 `Date.now() - pageOpenedAt`——这是「从打开录入页到入库」的**累计值**，不是「这一条的录入耗时」。实测 16 条连录得到 27.8s → 45.5s **单调递增、每条 +1.2s、均值 36.6s**（16 条全超 30 秒），而真实提交只花 200–402ms，**差两个数量级**。数字看着合理，所以一直没人怀疑。

修法：起点改为**本条的第一个输入动作**（`itemStartedAt`，在文本/网址/备注的 `input`、平台 `change`、选图/粘贴/点封面这些入口都 `markItemStart()`，`clearForm()` 时清零）。E5 增加 `ms_from: 'input'` 字段；`stats.a1()` **只认 `ms_from === 'input'` 的样本**，旧口径（无 `ms_from` 或 `'page'`）直接不进均值——两种口径的数不可比，宁可不计也不掺。

**P0-2　A2 把「AI 什么都没产出」算成「可用」**（已修）

`diff.js` 原判定 `changed = before !== after`。无图素材里 AI 的 `caption`/`ocr_text` 是空串、库里现值也是空串 → `changed = false` → 计入「没被改过 = 可用」。于是「AI 什么都没给」被读成「AI 给的东西可用」，**会自我实现的假通过**（16 条全无图的样本里这两个字段显示 100% 可用）。

修法：`diff.summary()` 每个字段同时给 `produced`（AI 产出过非空内容的样本，进分母）与 `empty`（没产出的，不进分母）；`stats.fieldRate()` 产出样本为 0 时按「样本不足」处理、不给百分比。与 #023 修的是同一类问题但**方向相反**：#023 是分母被做小、这条是**分子被虚增**。

**P1-1　存网址这条路产出的是一屏灰块 → 选 B，加服务端抓原文**（已修，本次最大改动）

v1 的采集方式定的是「截图 + 文本复制」，但用户的真实习惯是**存链接**（那份测试文件本身就是证据：16 条全链接、0 截图）。原实现从不尝试抓网页的标题或首图，AI 只能靠 URL 里的英文 slug 猜词，封面恒为空。

选 B：新增 `app/_page_meta.py`，`_serve.py` 挂 `/api/meta`、`/api/img` 两个端点。为什么必须服务端：gooood 不发 `Access-Control-Allow-Origin`，浏览器既拿不到对方 HTML 也拿不到图片**字节**，而「取字节 → canvas 缩 400px」必须同源。首图取回后复用截图那条管线缩成 400px 封面（`cover_source` 新增 `'link'`），标题/描述落 `page_title`/`page_desc`（供 AI 分类 + 重跑复用 + 进索引让「搜工作室名/项目原名」有效）。**抓取必须可失败**：对方站挂了或平台跑不了服务端，都降级成「没有封面」照常入库。

**安全**：`/api/meta`、`/api/img` 等于给公网一个「替我取这个 URL」的入口，SSRF 防护不是可选项——只放行 http/https、DNS 解析后逐地址校验只认公网地址（回环/私网/链路本地/云厂商 169.254.169.254 元数据口全拒）、手工跟随重定向逐跳重校验（上限 3 跳）、连接与读取超时 + 总预算、响应体按字节截断、校验 Content-Type。已知残留风险（DNS rebinding 的 TOCTOU 窗口）在 `_page_meta.py` 顶部如实记录，不假装解决。

**P1-2　「重跑 AI」入口会过期 + 失败原因不落库**（部分修，重跑入口本轮未动）

失败原因不落库的问题：`add.html` 的 `statusHtml()` 只在**本次会话内存**里有 `it._ai.error`/`it._ai.msg`，刷新后就只剩「上次识别没完成」。本轮未加 `ai_error` 列（涉及 schema 再改 + 详情页重跑按钮 + 卡片失败标记，体量不小），**留待后续**——先记录在案。重跑入口只在「最近 8 条」窗口里、详情页无重跑按钮，同属此列。

**P1-3　兜底横幅把原因归错了**（文案未改，根因已在提示词层缓解）

横幅说「落在其他/待归类超 10%，该调整抽屉划分」，但逐条看 4 条落兜底的：1 条是奖项榜单页（本就不该归）、1 条 AI 明说「待核实」判不出、2 条是 **AI 判错**——没有一条是抽屉定义有缺口。根因是「只填网址」时模型只有一串 URL 而提示词还在催它「判不出就落兜底」。**本轮修的是根因**：抓 `page_title`/`page_desc` 后模型有了真实文字，分类判错率应明显下降；横幅文案本身未改（等样本多了再看还要不要区分「判不出 vs 判错」两种信号）。

**P2-1　标签被稀释**（提示词层已修）

`案例参考` 出现 18/19 条（95%），作为筛选维度几乎无区分度；AI 还自造 `待核实`、`网页收藏` 这类**元信息标签**。本轮给提示词加硬约束「不要产出表示不确定或来源类型的标签」；`案例参考` 这类高频标签要不要「只在抽屉体现、不进标签」还没定，等抽屉复盘（A13）时一并处理。

**P2-2　`source_type` 语义错**（已修）

只填网址时原三元表达式把 `source_type` 算成 `'text'`。已补 `'link'`：`coverRaw ? (text||url ? 'mixed' : 'image') : (url ? 'link' : 'text')`。

**实测验证**：改完后重新录 3 条链接，`cover_source` 全部变 `'link'`、标题从原文抓取（如「青山学堂，杭州」而非 slug 猜的「青山自然学校」），证明 B 方案线上跑通（`/api/meta` 线上返回真实 og:title/desc/image）。库已清理回 6 条（原 3 + 保留 3）。

#### 10）摘要在卡片、设计说明总结在详情页（2026-09-22）

**需求来源**：用户看完首屏后提了两个问题——① 详情页「文本」卡片里那段文字到底起什么作用？② 与其让人手写，不如抓网页信息让 AI 做总结。第一条暴露的是**定位问题**：那张卡里的 `raw_text` 是「用户自己粘进来的原文」，只填网址的条目根本没有它，所以它时有时无、看不出规律。第二条顺着改下去，就成了这一轮的主线。

**决策链（每一步都由用户拍板）**：

| 问题 | 选项 | 结论 |
|---|---|---|
| AI 读什么 | 只读标题 + 描述 / 读网页正文 | **读正文**（`_page_meta.extract_body`）。只有读到「怎么做、效果如何」，才回答得了「值不值得点进原文」 |
| 总结的公式 | 由模型自由发挥 / 固定「理念 + 做法 + 效果」 | **固定三段**。用户原话：「我的总结更像是对网页它的整体设计说明的一个总结，如果我感兴趣我才会点进去看」 |
| 放哪里 | 替换掉卡片摘要 / 卡片留摘要、详情页放总结 | **乙**：两处各司其职。卡片要的是扫读（摘要管「认得」），总结只出现在已经点进来的地方（管「要不要看原文」） |

**实现（7 个文件 + 2 列 + 4 条数据）**：

- `_page_meta.py`：加 `MAX_BODY_CHARS=8000` / `_strip_chrome()` / `_balanced_blocks()` / `_blocks_to_text()` / `_drop_translation()` / `extract_body()`；`parse_meta()` 返回值加 `text`。
- `ai.js`：提示词加「网页正文」段与 `digest` 字段（`BODY_LIMIT=6000`）；新增 `normalizeDigest()`；`snapshot()` 记 `digest`。
- `add.html`：`createItem` 写 `page_text`；`runAi` 传 `pageText` 并回写 `ai_digest`。
- `store.js`：`ITEM_COLS` 加两列；`INDEX_COLS` 只加 `ai_digest`。
- `cards.js`：`_hay` 加 `ai_digest`（让「搜正文里的专有词」也能命中）。
- `diff.js`：字段表加「设计说明总结」。
- `item.html`：「设计说明总结」卡（所有者可改 / 访客只读三行）、三种空态、访客态视觉降级、窄屏保存条修复。
- 数据库：`items` 加 `page_text` / `ai_digest` 两列；5 条 URL 条目回填 `page_text`（1 条返回 403 抓不到，如实留空，降级路径被真实走到）；4 条重跑 AI。

**实测**：

| 条目 | 正文 | 总结 | 耗时 |
|---|---|---|---|
| id=28 高线公园 | 5,203 字 | 113 字 | 33.1 / 34.0 / **67.9** s |
| id=15 | 3,261 字 | 150 字 | 28.5 / 34.0 s |
| id=11 | 3,482 字 | 110 字 | 45.0 s |
| id=8 阿卡汗建筑奖 | 5,250 字 | 空 | 45.6 s |

id=8 总结为空是**正确行为**：那是一条获奖榜单页、不是设计项目，提示词要求此时给空串。界面显示「AI 读了正文，判这条不是设计项目」，**不显示空白**。

**顺手纠出来的两个事实错误**（机理见 4.4）：id=8 原摘要写「2025 阿卡汗奖」，正文里是「第 16 届」；id=11 原判「坡上…社区花园」，正文里是「雅加达商业区下沉式商业空间」——差得相当远。**读正文之前，摘要只能靠标题猜。**

**踩到并记在案的坑**：

1. **改了被 `_serve.py` import 的模块，必须重启服务。** `_page_meta` 是启动时 import 的，改完不重启，`/api/meta` 返回的还是旧结构（有 `desc`、没 `text`），很容易误判成「抽取没生效」。
2. **`loadIndex()` 与 `fetchItem()` 取的不是同一批列。** 拿 `loadIndex()` 查 `page_desc` 是否为空，得到「空」是假象（`INDEX_COLS` 根本没这列）。**查真值一律走 `fetchItem()`。**
3. **`position:sticky` 出不了包含块。** 窄屏保存条不贴底，不是 z-index 也不是高度的问题，是它的包含块 `.col-fields` 在单列布局下起点位于首屏之下。
4. **本轮最贵的一次误判**：60s 超时把 id=28 掐断，第一反应是「提示词变长了」→ 做长短提示词对照 → **两条都 34s** → 才回到 4.3 的老结论「方差来自服务端排队」。**若当时直接去缩正文，就会为一个不存在的瓶颈丢掉「效果」段。**
5. **测试残留要回查数据库，不能只看界面。** 界面显示「已还原」不等于库里已还原——本轮收尾时查出 id=2 的 `ai_digest` 仍留着测试标记「（测试）」，已清空。这与 #018「测试数据污染」是同一类问题，值得当成固定收尾动作。

**验证**：6 个页面 0 控制台报错；访客态 `#fields` 内 0 个输入控件；保存链路（改动 → 保存 → 提示 → 落库 → 还原）跑通；搜索「ETFE」（只出现在正文里的词）命中 1 条。

**发布（2026-09-22）**：`tools/build-deploy.py` 组装 18 个文件（三条自查全过）→ **复用应用 ID `wbapp_az0Z1pxT1CjCvbUNffduqc`** 上传，发布件类型 `http-service`，入口 `python3 _serve.py`，链接不变（https://elangit.app.workbuddy.host/ ）。发布前先在本机以发布方式起服务预检（各页 200、`/_verify_*` 404、`no-store`、首页 7 条素材、`/s/令牌` 200 = share.html、0 报错），发布后线上逐项核到新代码（`aiTimeoutMs: 90000`、`ai.js` 有 `digest`、`item.html` 有「设计说明总结」、`store.js` 有 `ai_digest`、`stats.html` 字段顺序有 `digest`、`_page_meta.py` 有 `extract_body`），详情页所有者态实测显示「总结依据：抓到的网页正文 5203 字」。**验证件线上 404，没上公网。** 公开源码仓库走另一条流水线（`push-to-github.py --export-to`，脱敏），已本机提交、**推送被网络拦（`github.com:443` 代理 502）未完成**。

---

## 10. 评审时要你拍板的事项（历史记录）
| # | 事项 | 结论 |
|---|---|---|
| **R-1** | 所有者保护：接受前端口令的降级（快），还是上单所有者账号（稳）？ | 2026-09-19 决定接受降级；2026-09-20 进一步确定为「纯本地口令」，哈希不入库 |
| **R-2** | 界面稿的视觉方向（配色、卡片密度、信息层级）是否认可？ | 2026-09-19 已认可 |
| **R-3** | 是否现在开工？ | 2026-09-20 确认开工 |
| **R-4** | 图片存储走哪条路？ | 2026-09-20 选定**路径 A**（图片 base64 存数据库，不启用账号体系） |
| **R-5** | PRD F1-4 原写「AI 识别 ≤10s」，实测封不住（3–58s，方差由服务端主导）。是否改写为「本地 ≤1s 出图；AI 识别异步补齐，超时即降级为手填」？ | 2026-09-20 用户确认**改写**，PRD 升 v6（F1-4 与 7.1 录入流程、耗时预算一并改）。超时阈值依实测定为 **60s**，**2026-09-22 上调至 90s**（见 R-6） |
| **R-6** | AI 超时阈值：v13 起要让模型读网页正文，是否维持 60s？ | **这一条未经你拍板，是我直接改的**（技术参数，不是需求变更，所以没停下等你）——第三次实测 67.9s 被 60s 掐断、结果直接丢失。先做了「缩正文换速度」的对照：长短提示词耗时基本一致（都约 34s），证明瓶颈在服务端排队、不在正文长度，于是**上调至 90s**（PRD 同步升 v13）。**你若认为这个该由你定，直接说，我改回去。** 另：若之后仍出现被掐断的条目，说明 90s 也不够，届时应考虑把 digest 拆成第二次短调用，而不是继续加时间 |

---

*v13，2026-09-22。**摘要在卡片、设计说明总结在详情页**（新增 4.4 与 9.8 第 10 条）：`_page_meta.py` 新增 `extract_body()`（去导航/操作位噪声 → 标签配对计数取主体 → 双语站去英文半篇 → 上限 8000 字），`/api/meta` 返回值新增 `text`，落库为 `items.page_text`；`ai.js` 提示词新增「网页正文」段与 `digest` 字段（`BODY_LIMIT=6000`，依据是 4 条真实文章正文 3,304–5,363 字且「效果」段在文末），新增 `normalizeDigest()`；素材表新增 `page_text` / `ai_digest`（`INDEX_COLS` 只收 `ai_digest`，`page_text` 不进索引也不进检索范围）；`diff.js` 字段表新增「设计说明总结」（v13 前的条目 `ai_raw` 无此键 → 不进 A2 分母）；`item.html` 新增「设计说明总结」卡（三种空态）并做**访客态视觉降级**（只读视图、去掉对访客无意义的操作引导、chip 不再显示可点手势）；修掉窄屏保存条不贴底（`sticky` 受包含块限制 → 改 `fixed` + `main.detail` 补下内边距）；`config.aiTimeoutMs` 由 60s 提到 **90s**（实测 33.1/34.0/67.9s，且长短提示词耗时一致 → 瓶颈在服务端排队，缩正文无效）。另记两条订正：**PRD F5-1「全部原图」不成立**（只填网址的条目库里没有原件，只有一张封面），**PRD F4-1 检索范围补入设计说明总结、明确排除 `page_text`**。*

*v12，2026-09-21。**首次真实录入测试（16 条 gooood 链接）发现的 8 个问题修正**（新增 9.8 第 9 条）：**F1 补「网址采集」**——新增 `app/_page_meta.py`（服务端抓原文标题/描述/首图，纯标准库、SSRF 防护：只放行公网地址、逐跳重校验、超时与字节上限），`_serve.py` 挂 `/api/meta`、`/api/img` 两个端点，`add.html` 提交前抓取、封面复用截图管线缩成 400px（`cover_source` 新增 `link`）；**A1 计时起点改为「本条第一个输入动作」**（`itemStartedAt`，`ms_from: 'input'`，`stats.a1()` 只认 input 样本）；**A2 分母改为「AI 实际产出非空的样本」**（`diff.summary()` 每字段同时给 `produced`/`empty`，`stats.fieldRate()` 产出为 0 按样本不足处理）；素材表新增 `page_title`/`page_desc`（`source_type` 新增 `link`）；AI 提示词补「不要产出表示不确定/来源类型的标签」。3.1 补 `page_title`/`page_desc`，第 9 节代码结构补 `_page_meta.py` 并更新 `add.html`/`_serve.py`/`store.js`/`diff.js`/`ai.js`。*

*v11，2026-09-20。**任务 9 发布上线**（新增 9.8 第 8 条）：应用已部署在 https://elangit.app.workbuddy.host/ ，平台以单端口 HTTP 服务方式跑 `_serve.py`（因此路径式分享链接 `/s/令牌` 的服务端重写在线上成立）；发布前用「同文件、同进程、只换来源」的对照实验测出**云服务强制 Origin 精确匹配**，据此确定「必须复用应用 ID、不得新建应用」；新增构建步骤 `tools/build-deploy.py`（发布件 = `app/` 减去一次性验证件，并自查逐字节一致、验证件未混入、key 保留），同时写明它与 `tools/push-to-github.py` 在凭据处理上方向相反；`_serve.py` 改 `ThreadingTCPServer`；第 5 章与第 9 节的完成状态同步到任务 9。*

*v10，2026-09-20。A1/A2/A4/A13 的口径修正（外部复审提出、逐条回读代码确认成立后修）：新增 **E14 `search_attempt_end`** 与 `track.js` 的检索结算（`beginSearch` / `resolveSearch` / `abandonSearch`，见 9.8 第 7 条）；新增 `store.loadFirstEvents()`（A1 要「最早 20 条」，样本窗口必须是查询的属性）；`diff.summary(items, {firstN})`（A2 的「前 50 条」）；A13 改三态、新增中性状态「待人工复盘」；新增 `app/stats.js`（门槛与统计的纯函数，为的是让临界值可断言），四个渲染函数统一走它；顺手修掉 `REPORT_COLS` 少取 `ai_summary / ai_caption / ocr_text` 导致 A2 字段表三行可用率恒为 0% 的潜伏 bug。3.8 补 E14 与「14 条实写 10 条」，第 9 节代码结构补 `stats.js` 并更新 `track.js` / `stats.html`、数据页取数口径表。**（本次一并订正头部版本号：原写 v8，而下方版本记录已到 v9。）***

*v9，2026-09-20。新增 3.8（events 表，以及「哪 4 条事件不写、为什么」的派生判据）、9.8（F2-8 改为派生而非落库；A14 根因是 `rect && cover_index===0` 这个守卫、矩形改按图存；E10 不做的理由；埋点失败必须旁路；E7 跳转前发请求被取消与 `session()` 顺序写反两个静默丢数的坑；数据页取数口径）；3.2 新增 `item_images.ai_rect`；第 9 节代码结构补 `diff.js` / `track.js` / `stats.html`，任务 8 标记完成并订正「差异落库」的旧说法。*

*v8，2026-09-20。重写第 5 章（分享的实现）——原文写的是「只返回标记为可分享的素材」，与实装的「整个收藏夹一个开关」不符，已按实际行为改写；新增 9.6（分享页的令牌四种入口、`<base>` 注入修路径 404、`.empty` 类名撞车）与 9.7（F7-4 的 localStorage 计数与模块级定时器）；第 9 节补任务 7 完成状态与新增文件 `share.html` / `cards.js`。*

*v7，2026-09-20。3.1 新增 `items.ai_title`（含为什么、空串与 NULL 的语义区别、存量补名只写单列的取舍）；新增 9.4（卡片信息顺序是宽度算术的结果，不是审美调整）与 9.5（顶栏窄屏挤坏的两处修法，以及「`set viewport` 不重新加载导致误判 CSS 未生效」这个坑）。*

*v6，2026-09-20。新增 9.2（详情页形态、两类保存时机、F5-8 三件事、sticky 导致按钮够不着的实测 bug）与 9.3（口令门禁提前落地、权限判断只留一处、写操作在 store 层设卡）；3.1 新增 `items.ai_raw` 并订正 `cover_source` 取值（`manual` → `none`）；第 9 章补任务 6 完成状态与新增文件。*

*v5，2026-09-20。新增 9.1（浏览页两步取数、侧栏必须包含 AI 新造标签、筛选一律 AND）；第 9 章补任务 5 完成状态、代码结构补 `add.html` / `style.css` / `_serve.py`，并订正录入页文件名（`index.html` → `add.html`）。*

*v4，2026-09-20。新增 4.3（首字延迟实测、提示词配对实验、并发结论订正、裁切精度噪声、录入实装实测）；第 9 章补任务 4 完成状态与应用代码结构；订正 3.3 抽屉余量（6 条不是 7 条）。*
